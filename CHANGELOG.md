## 2026-06-15

### AionCore

- **Feat(terminal): Migrated terminal backend from Electron node-pty to Rust portable-pty** — new `aionui-terminal` crate provides cross-platform PTY management with WebSocket transport. Terminal sessions now persist server-side and survive renderer crashes.
- **Feat(terminal): WebSocket-based terminal transport with JSON message protocol** — client and server communicate via JSON messages (`TerminalWebSocketMessage` for input/resize/ping, `TerminalOutputMessage` for output/error/exit/pong).

### AionUi

- **Feat(terminal): Replaced xterm.js frontend with ghostty-web** — migrated from xterm.js 5.5 to ghostty-web for better performance and WASM-based rendering.
- **Feat(terminal): HTTP REST API for terminal session management** — new endpoints for create/list/kill/resize operations via HTTP API.
- style: warm global shadow tokens to Chisl ink palette — changed light theme shadow RGB from (60, 56, 54) to (42, 38, 32) and dark theme from (0, 0, 0) to (20, 16, 12) in base.css
- fix(command-center): prevent guide page overflow by enforcing chat pane min-width — added `minWidth: 'var(--app-min-width, 360px)'` to the center chat pane div in Layout.tsx to prevent the New Chat guide page from being squashed below 360px when the Command Center editor or conversation panes are resized too wide
- Extracted `Surface` primitive in `components/base/Surface.tsx` (Phase 2)
- Migrated 4 AgentSettings shells to use Surface: AgentHubModal, AgentCard, RemoteAgentManagement, ProviderAuthCard

## 2026-06-13

### AionUi

- Rebranded from AionUi to Chisl: updated productName, executableName, appId, and package descriptions
- Reduced UI density across the app to engineering-tool levels: tightened modal headers (40→32px), accordion headers (30→22px), ConversationPane header (44→32px), sendbox controls (32→26px), and reduced padding/margins on tabs, collapse panels, dividers, forms, approval cards, tool group summaries, and file tree.
- Defined `--font-ui` and `--font-mono` CSS custom properties in base.css, replacing hardcoded font-family stacks in arco-override.css and ShadowView.tsx to establish a single source of truth for typography.
- Fix(login): replace hardcoded Tailwind hex colors in error/success message styles with semantic `--danger`/`--success` tokens and `color-mix` tints so messages are readable in dark mode
- Chore(desktop): remove unused root CSS scratch files (test.css, workspace-guides.css, workspace-styles-add.css, workspace-styles-refine.css)
- Refactor(login): replace hardcoded brand-color box-shadows and font-family with token-derived values. Focus rings and submit-hover shadow now use `color-mix(in srgb, var(--brand) X%, transparent)` instead of hardcoded `rgba(180, 72, 12, X)`. Card shadow uses `var(--shadow-lg)`. Font family uses `var(--font-ui)`.
- Refactor(theme): migrate all remaining `--color-text-*` Arco legacy tokens to Chisl canonical `--text-*` tokens across 20 files (41 occurrences). Replaced `--color-text-1` → `--text-primary`, `--color-text-2` → `--text-secondary`, `--color-text-3` → `--text-tertiary`, `--color-text-4` → `--text-disabled`. Updated catppuccin.css preset to bridge form, colors.ts mapping utility to canonical tokens, and ShadowView.tsx shadow-DOM propagation to read `--text-*` tokens. This completes Phase 1 of the Arco→Chisl token migration (Plan 10).
- Refactor(theme): migrate all `--color-bg-*` and `--color-border-*` Arco legacy tokens to Chisl canonical tokens across 42 files (~160 occurrences). Replaced `--color-bg-1` → `--bg-1`, `--color-bg-2` → `--bg-2`, `--color-bg-3` → `--bg-3`, `--color-bg-4` → `--bg-4`, `--color-bg-5` → `--bg-5`, `--color-bg-popup` → `--bg-1`, `--color-border-1` → `--border-light`, `--color-border-2` → `--border-base`, `--color-border-3` → `--bg-4`, `--color-border-4` → `--bg-5`. Updated catppuccin.css preset to bridge form and colors.ts mapping utility for bg-1 through bg-5. Intentionally skipped --color-bg-0/6/8/9/10 (no canonical equivalent). This completes Phase 2 of the Arco→Chisl token migration (Plan 10).
- Refactor(theme): migrate all remaining `--color-fill-*`, `--color-primary-*`, and `--color-danger/success/warning-*` Arco legacy tokens to Chisl canonical tokens across ~30 files (~57 occurrences). Replaced `--color-fill-1/2/3/4` → `--bg-1/2/3/4`, `--color-primary` / `--color-primary-6` / `--color-primary-1` → `--brand`, `--color-danger` / `--color-danger-6` → `--danger`, `--color-warning-6` → `--warning`, `--color-success-6` → `--success`. Converted scaled Arco variants to `color-mix()` expressions: `--color-primary-light-1` → `color-mix(in srgb, var(--brand) 8%, transparent)`, `--color-primary-light-3` → `color-mix(in srgb, var(--brand) 20%, transparent)`, `--color-primary-light-4` → `color-mix(in srgb, var(--brand) 28%, transparent)`, `--color-danger-light` → `color-mix(in srgb, var(--danger) 8%, transparent)`, `--color-success-light-1` → `color-mix(in srgb, var(--success) 8%, transparent)`. Intentionally skipped miscellaneous tokens (`--color-aou-*`, `--color-guid-*`, `--color-mask-*`, `--color-white-to-black`, `--color-neutral-*`, `--color-bg-6`) with no clear Chisl equivalents. This completes Phase 3 of the Arco→Chisl token migration (Plan 10).

## 2026-06-12

### AionUi

- **Docs(agents): add "Operating Rules (HARD)" section to AGENTS.md and remove `git push` from workflow.** Codifies the hard rules an AI agent must follow: no push / no force-push / no history-rewrite / no branch creation / no merge-to-main / no `upstream` interaction without explicit per-operation user approval; secrets handling that explicitly forbids `git filter-repo`/BFG/filter-branch (the "fix" caused today's SHA divergence); scope discipline; a plain-English numbered operator test-plan requirement when the agent cannot self-validate; test discipline (no delete/weaken/`.skip`); restrictions on creating planning `.md` files and "PR description" comments; stop-and-ask triggers for unexpected git state, looping debug, and scope creep; shell hygiene (no `git add -A`, no `--no-verify`); and honest-reporting requirements for end-of-task summaries. The old `### Pushing` subsection (which instructed `git push` and referenced `just push`) is renamed to `### Committing` and the push step removed — pushing is now explicitly the user's job, cross-referenced to the new Git Safety section. Driven by an incident today where an agent reset `main`, force-pushed multiple times, invented `matt/refactor/layout-*` feature branches, self-merged into `main`, and ran a history purge that broke SHA-based divergence comparison with upstream (recovered by deleting 302 inherited GitHub branches + 228 tags and restoring from `backup/main-before-reset`).

### AionCore

- **Docs(agents): add "Operating Rules (HARD)" section to AGENTS.md.** Same set of hard rules as AionUi, with Rust-adapted examples (clippy/fmt instead of lint/format, `#[ignore]` instead of `.skip`, `assert_eq!` instead of `expect().toBe()`). New section sits above the existing `## High-Priority Rules` (AcpAgentManager field-addition guard, logging conventions) and `### Test Failure Handling` — those project-specific rules are preserved as-is and the new test-discipline subsection cross-references them. Same incident driver as the AionUi entry.

- **Refactor(layout/ui): P3 cleanup — accessible resize handles, custom CSS hook, sendbox de-Arco, token migration, and consistency polish (#3 #8–#12 #14–#17 #20 #21 #23 #25 #26 #34 #35 #36).** Extracted Layout's custom CSS load/heal/inject logic into `useCustomCssInjection` / `useCustomCssStyleInjection` and kept the P2 route-gating behavior (initial mount + storage/custom-css events + settings enter/leave only). Added a shared accessible `ResizeHandle` (`role="separator"`, keyboard resize, visible focus/hover/drag affordance) for the main sider and workspace-section dividers; `SiderWorkspacePanel` now uses a pane ref map instead of scanning children by `data-testid`. Replaced fully repainted sendbox Arco buttons (send/stop/slash/voice/speech) with native buttons, dropping `sendbox.css` `!important` usage from 67 to 22; scoped the placeholder override to `.sendbox-panel`, fixed the speech button idle radius, and kept the still-Arco attach button visually aligned with the flat rail controls. Moved stray Arco input/select theming from `layout.css` to `arco-override.css`, remapped dark tag colors to semantic tokens, demoted secondary seams, scoped the global Arco active-scale out of dense chrome, unified conversation-pane collapse iconography, added explicit show/hide sidebar/conversation labels in all locales, restored `unit.minute_short`/`unit.second_short`, and migrated the largest `--color-*` CSS consumers (`guid/index.module.css`, `sendbox.css`) to Chisl semantic tokens with a theme README note that `--color-*` is Arco-internal. During manual testing a major P3 regression was found and fixed before commit: the new main sider resize handle initially sat in normal flex flow with `height: 100%`, stealing all sider height so file tree/diff/history/outline and settings menus disappeared; it is now an absolute edge overlay (`variant="edge"`). A second sider layout bug was also fixed: ref-map wrapper divs around `SiderAccordionSection` broke the accordion flex-child contract, so refs now attach directly to the sortable section via `elementRef`.
- **Fix(layout): restore main sider resize handle behavior after P3 standardization drift.** The shared `ResizeHandle` mixed pointer capture with the existing `useSiderResize` window `mousemove`/`mouseup` drag flow, causing the sider width to stay latched to the cursor after mouse release and making the handle shape feel different from the rest of the app. The main sider handle now uses the proven direct `onMouseDown` overlay again (12px hit area, 2px line expanding to 6px brand on hover/focus/active) while keeping the new `role="separator"` and keyboard arrow/Home/End resizing. Workspace section handles continue to use the shared component.
- **Fix(layout): make left sider workspace sections compact and overflow-safe.** Explorer/Diff/Outline/Timeline section headers were 44px tall and section resizing used a 44px minimum plus fixed `flex: 0 0 auto` heights, so custom sizes felt locked and multiple expanded panes could push lower sections (Outline/Timeline) out of view on short displays. Headers are now 30px tall with tighter padding/actions; section resize deltas are clamped against both adjacent panes so total height stays stable; user-sized sections use a shrinkable flex-basis so expanded bodies compress toward their compact header instead of pushing later sections off-screen.
- **Fix(layout): auto-collapse left sider workspace sections instead of squeezing all expanded panes.** The previous overflow fix kept every expanded Explorer/Diff/Outline/Timeline pane visible by shrinking bodies into unusable slivers. Workspace section expansion is now controlled at the panel level: each expanded pane keeps a usable minimum body height, and when the stack cannot fit the available sider height, the panel collapses sections by policy. Normal overflow collapses the bottom-most expanded section first; if the user explicitly opens the bottom-most Timeline section while upper sections are expanded, Timeline stays open and the top-most expanded section collapses instead. Per-section expanded state still persists to the existing localStorage keys, resize deltas remain clamped so total height stays stable, and compact 30px headers remain accessible.
- **Refactor(layout): P2 batch — extract `useSiderResize`/`useMobileViewport`/`useGuidSkills`, gate custom-CSS healing, mobile sider transition, visual polish (#30 #31 #32 #33 #1 #2 #13 #18 #19).** Layout.tsx shrinks 700→~530 lines: the entire sider-resize feature (width state + localStorage persistence, drag refs, CSS-var lifecycle incl. the no-op-drag re-apply fix, icon-only threshold commits) moved to `hooks/system/useSiderResize.ts`; viewport/mobile detection moved to `hooks/system/useMobileViewport.ts`; GuidPage's skills catalog + toggle lists + preset-sync effect moved to `pages/guid/hooks/useGuidSkills.ts` (GuidPage no longer owns 3 useStates + 2 effects for it). Custom-CSS re-healing no longer runs 3 configService.get calls on EVERY route change — a `prevPathnameRef` gates it to settings-route enter/leave transitions (initial mount, `custom-css-updated`, and `storage` event paths unchanged). Mobile left sider now animates open/close (`transform var(--motion-slow) var(--motion-ease-emphasized)`) via `layout.css` classes with a `.layout-sider--mounted` rAF guard against first-paint animation, a dragging exception, and `prefers-reduced-motion: reduce` support — previously it snapped (`transition: 'none'`) while the right pane animated. Designer pass: `.message-agent` slab background → transparent (the 2px rust edge now carries identity; sticky author gradient retargeted `--bg-base`→`--bg-1` to match the newly revealed surface), chrome hover overlays tokenized `rgba(0,0,0/255,...)` → `var(--bg-hover)`/`var(--bg-active)`, raw transition durations → `--motion-fast`/`--motion-slow` tokens in titlebar/pane/messages CSS, responsive titlebar pill density (segment padding tightens, label ellipsis at narrow widths; no label/i18n change). Pre-commit oracle review caught and fixed before commit: (1) CRITICAL — the extracted auto-collapse effect gained a `collapsed` dep and slammed the mobile sider shut the instant a user opened it (now guarded by `prevIsMobileRef`, fires only on desktop→mobile transition); (2) HIGH — five mobile-only CSS rules (incl. `.arco-message-wrapper { padding-left: 0 }` which killed the desktop toast offset, and `!important` font-size bumps hitting desktop chat history) had escaped the `@media (max-width: 767px)` block during the transition refactor — restored inside; (3) dead `siderStyle`/`siderClassName` bindings removed and `layout-sider--mobile` class now correctly gated to mobile only; (4) two tautological useSiderResize tests now pre-seed the CSS var before asserting it clears. 27 new unit tests (13 sider/viewport hooks, 7 guid skills, plus reworked assertions); full suite at pre-existing-failure baseline (better-sqlite3 native mismatch et al.), zero new failures.
- **Perf(renderer): smooth streaming-chat rendering — frame-aligned batching, block-level markdown memoization, streaming-aware tail rendering.** Streamed responses were visibly choppy: message batching used `setTimeout` macrotasks (multiple React commits per frame), every chunk re-parsed the ENTIRE accumulated markdown through remark/rehype/KaTeX (O(n²) across a stream), `CodeBlock` re-tokenized whole code blocks per chunk with no memo, and `MessageText` re-ran `JSON.parse` + regex passes on full content per chunk. Fixes: (1) `useAddOrUpdateMessage` now flushes via `requestAnimationFrame` with a 50ms `setTimeout` fallback (rAF is suspended in hidden Electron windows; whichever fires first cancels the other) and the old self-re-arming idle poll is gone; (2) new `splitMarkdownBlocks` splits content into prefix-stable blocks (fence/`$$`-math/list-aware, lossless `join('\n')` invariant) and `MarkdownView` renders each through a memoized `MarkdownBlock` so only the growing tail re-parses — gated on `isStreaming` so settled/static markdown keeps single-pass semantics (footnotes/reference links unaffected app-wide); (3) new `is_finished` flag on `IMessageText` content threaded through `useRemoteMessage` (deltas marked in-flight; finalized on `finish`/`error`/`session_idle`, on mid-turn msg_id switches, and on `start` as a dropped-event safety net) — the streaming tail renders code as plain `<pre>` and skips KaTeX until the turn completes, then snaps to full quality exactly once; (4) `CodeBlock` wrapped in `React.memo` with memoized KaTeX/`formatCode`/line-split work, hooks restructured above conditional returns; (5) `MessageText` memoizes `parseFileMarker` and short-circuits `JSON.parse` for non-JSON content. ACP/Gemini platforms are unaffected (`is_finished === undefined` = not streaming). Oracle-reviewed; 30 new unit tests (splitter lossless + prefix-stability property tests, rAF/fallback race, merge-flag propagation). Plan validated against source first — two findings from the original analysis (Shadow DOM overhead, "memoization failure") were disproven and dropped.
- **Fix(renderer): scroll-up during streaming no longer snaps back to bottom.** The frame-aligned auto-follow refreshed `useAutoScroll`'s 150ms programmatic-scroll guard every frame, so the delta-based user-scroll detection in `handleScroll` was permanently starved — upward scrolls were discarded as Virtuoso adjustments and the next frame snapped back; users could not scroll up until the response finished. User intent is now detected from input events, which never fire for programmatic `scrollTop` writes and therefore bypass the time guard: `wheel` with `deltaY < 0` stops following immediately; scrollbar/touch drags (pointer held + upward scroll > 10px, thresholded so Virtuoso's small internal adjustments don't trip it during mid-stream text selection) do the same. Scrolling back to bottom re-engages auto-follow via the existing `atBottomStateChange` reset.
- **Refactor(renderer): extract sider-logic hooks and deduplicate SidebarIcon (#28 #29 #24).** `useMainProcessLogBridge` and `useTrayEventHandlers` pulled out of Layout.tsx into `hooks/system/`, mirroring the `useDeepLink`/`useNotificationClick` pattern. SidebarIcon deduplicated into `components/layout/icons/SidebarIcon.tsx` (keeps the Titlebar version's explanatory viewBox comments and the inline-block vertical-align style). Both Layout.tsx and Titlebar/index.tsx now import the shared component.
- **Perf(renderer): sider drag resize no longer re-renders context consumers 60x/sec (#27).** During mousemove the live width is written to a CSS variable (`--layout-sider-width`) on `document.documentElement` instead of calling `setDesktopSiderWidth` per frame; the ArcoLayout.Sider style override uses `var(--layout-sider-width)` mid-drag so the visual resize happens outside React. The committed width (and icon-only threshold) update only once on mouseup. `LayoutContext.Provider` value wrapped in `useMemo` to prevent spurious re-renders from inline object recreation.
- **Fix(renderer): `.arco-message-wrapper` hardcoded `padding-left: 266px` replaced with `var(--layout-sider-width, 0px)` (#5).** Toast positioning now tracks the sider width through the same CSS variable, including collapsed/mobile states (var is cleared when collapsed or on mobile, falling back to 0px).
- **Fix(layout): dedupe system hook calls and keep sider width var in sync on drag end.** `useMainProcessLogBridge` and `useTrayEventHandlers` were each called twice in Layout.tsx (top grouping + later extracted site), doubling main-process log bridges and double-firing tray events (including duplicate `task.stopAll` IPC calls). Removed the top-duplicate invocations so each hook runs exactly once at its preserved site (~line 385/388). Also fixed a no-op-drag bug in `endDrag`: it previously called `clearSiderWidthVar()` unconditionally and relied on React re-running the sync effect to restore the var, but a same-width drag (e.g. clicking the resize handle) caused `setDesktopSiderWidth` to bail out and the var stayed removed — `.arco-message-wrapper` then fell back to `padding-left: 0px` while the sidebar was open. `endDrag` now calls `applySiderWidthVar(finalWidth)` when the sider is open on desktop, so the var is always correct regardless of whether the state update bails out. The sync effect remains the single owner for collapse/mobile transitions (clears the var when collapsed or mobile).
- **Refactor(layout): terminal now spans full width across editor + chat + conversation pane.** Previously TerminalPanelHost was nested inside ArcoLayout.Content, a peer column to CommandCenterEditorHost, so the terminal only spanned the chat column while the editor ran full height — making the editor feel dominant. Restructured Layout.tsx so TerminalPanelHost wraps a flex row of editor + chat content + conversation pane, giving the terminal full-width span below all three panels.
- **Fix(layout): guid page stays dynamically centered as terminal height increases.** Added `justify-center` to the chat content div so the guid landing page centers vertically within the available space. When the terminal grows and the available space shrinks, the guid content remains centered instead of being pushed up out of view. Long conversation content continues to scroll normally.

## 2026-06-11

### AionCore

- **Fix(plugin): eagerly start plugin webserver on first remote agent session.** End-to-end testing showed `run_shell_streaming` failed with "Streaming failed" after a Chisl restart because the plugin webserver on :64921 was never started — it only came up when the user opened the Install Plugin settings panel (snippet generation) or spawned a local OpenCode. Added process-global token validator (`plugin/auth.rs::set_global_validator` / `global_validator`) installed at app boot from `services.rs`; `ensure_local_fs_mcp` now calls `ensure_plugin_server` right after registering the shell approver when a remote agent is present. The server is a process-wide singleton (OnceLock + Mutex), so subsequent calls are no-ops. 80 plugin tests + 81 agent tests green.
- **Fix(local-fs): destructive file ops (`delete_file`, `rename`) now require per-action user approval.** End-to-end testing showed the remote model could delete local files with zero prompts by calling the local-fs MCP `delete_file` tool — only `run_shell` was gated by the ShellApprover. Added `ShellApprover::approve_fs_action` (default impl delegates to the shell prompt for back-compat); `RemoteShellApprover` overrides it with a file-change-specific confirmation card ("Delete a file on your machine?") whose `fsop-` call-id prefix routes an "always" reply to a SEPARATE approval-memory key (`fs_destructive`) — "Skip shell prompts" can no longer silently waive file deletions, and vice versa. Both tools fail closed without an approver and are hidden from `tools/list` alongside `run_shell` (new `APPROVAL_GATED_TOOLS`). `write_file` deliberately stays ungated (per-call Git snapshots cover recovery; prompting every edit would make coding unusable). 4 new gating tests incl. reject-does-not-delete; 809 lib tests green.
- **Fix(local-fs): steer the model toward `run_shell_streaming` for live output.** E2E testing showed long commands rendered only after completion: the model always picked the buffered MCP `run_shell` because nothing advertised the plugin's streaming tool. System hint now says to PREFER `run_shell_streaming` when present (output streams live) and documents that `aionui-local-fs_run_shell` is buffered; the `run_shell` tool description says the same. Also documents the new delete/rename approval so the model never claims a deletion happened before the tool returns.
- **Fix(plugin): `resolve_plugin_port` honors `AIONUI_PLUGIN_PORT` env override.** Production default stays 64921; lets tests (and operators) bind an alternate port. Fixes `ensure_loopback_plugin_endpoint_returns_loopback_url` failing with "Address already in use" whenever a dev aioncore instance held the fixed port.

### AionUi

- **Chore(security): purged `opencode-backup.json` from ALL git history and force-pushed.** The config-backup export embeds auth tokens (`includeAuth` section); a token-like string had been committed and pushed across 10+ commits on `main` and `feat/conversation-pane-moe`. Purged with `git filter-repo --invert-paths`, force-pushed both branches to `schlambos/chisel-ui`, and gitignored the file in both AionUi and AionCore. Stashes were preserved (filter-repo rewrote them; backup branches `stash-backup-0/1` kept as belt-and-suspenders). NOTE: GitHub may retain unreachable commits server-side for a while — rotate the exported tokens to be fully safe.
- **Fix(opencode-plugin): TRUE root cause of "Plugin export is not a function" — OpenCode resolves the entry from `package.json`, not the spec path.** OpenCode ≥1.17's `createPluginEntry` finds the `package.json` adjacent to a `file://` plugin spec and resolves the module from `exports["./server"]` → fallback `main`, completely ignoring the spec file. With `main: ./dist/index.js`, every prior entry-file fix (v1 shape, single-function wrapper) was bypassed and the barrel kept loading. Fixed by adding `"./server": "./opencode-entry.mjs"` to `exports` and creating `opencode-entry.mjs` in the package source of truth (`packages/opencode-plugin/`) — previously it lived only in the Docker repo's vendored copy, where `vendor-chisl-plugin.sh`'s `rm -rf` + rsync would have deleted it on the next re-vendor. Also added to `files` for npm publish. Verified end-to-end: container log clean after restart, AionCore `plugin hello plugin_version=0.2.0 hook_count=6`, Chisl UI sustained "Connected, v0.2.0, 6 hooks active".
- **Fix(web-host): stable backend port 25808 and fixed plugin dial-back port 64921.** `resolveBackendPort()` prefers `AIONUI_BACKEND_PORT` env, then the stable default 25808, falling back to an ephemeral port only when taken; spawn env now forwards `AIONUI_PLUGIN_PORT=64921` to the aioncore child. Remote plugin installs (Docker `AIONCORE_URL`) no longer break on every Chisl restart because the dial-back URL stays constant. Install guide documents the stable-port behavior and the `{data_dir}/plugin-listen-port` persistence.

### opencode-server-docker

- **Fix(docker): vendored plugin `package.json` gains `exports["./server"] → ./opencode-entry.mjs`.** Companion to the AionUi root-cause fix above — without it, OpenCode loads `main` (`dist/index.js` barrel) regardless of the jsonc spec path. The live container was hotfixed in place (sed on `/opt/chisl-opencode-plugin/package.json` + removal of the stale `plugins/chisl.mjs` dual-load loader); this commit makes the fix permanent for future image builds.

### AionCore

- **Fix(plugin): bearer-token auth and fixed listen port for the plugin webserver.** New `manager/remote/plugin/auth.rs` maps `Authorization: Bearer` headers to a `remote_agent_id` via SQL lookup on the `plugin_token` column; new `port.rs` pins the plugin webserver to `DEFAULT_PLUGIN_PORT` 64921 (new constant in `aionui-common`) so remote Docker installs have a stable dial-back target. Registry probe-hello guard and `config_snippet` → `opencode-entry.mjs` fixes (documented below) committed alongside.

### opencode-server-docker

- **Fix(docker): Chisl `opencode-entry.mjs` exports one async function from `capabilities.js`.** v1 `{ id, server() }` and re-exporting `dist/index.js` still hit "Plugin export is not a function"; entry now default-exports a single wrapper that imports `createPlugin` from `dist/capabilities.js` only.
- **Fix(docker): Chisl `opencode-entry.mjs` uses OpenCode v1 `{ id, server() }` shape (superseded).** Re-exporting `dist/index.js` default still failed with "Plugin export is not a function" (Bun/OpenCode resolved the barrel as a non-callable module namespace). Entry file now default-exports `{ id, server }` that dynamic-imports the real factory. Entrypoint quarantines broken `plugins/config-backup.ts` on start.
- **Fix(docker): SSE reachability probe no longer crash-loops the container.** `probe_aioncore_reachability` GETs `/plugin/events`; curl exits 28 on `--max-time` while the SSE body streams. Under `set -e`, that non-zero exit inside command substitution killed the entrypoint before `opencode serve` — container never reached "Starting OpenCode server". Probe now wraps curl in `set +e` / `set -e` so timeout is non-fatal; HTTP 200 before timeout still logs success.
- **Fix(docker): entrypoint probe no longer poisons AionCore plugin registry state.** The startup reachability probe previously POSTed `{"protocolVersion":1,"pluginVersion":"0.0.0","hooks":[]}` to `/plugin/hello`, which AionCore's `PluginRegistry::record_hello` accepted as a real hello — overwriting any prior real plugin metadata (v0.2.0, 6 hooks) with v0.0.0/0-hooks. This caused the Chisl UI to briefly show "Plugin v0.0.0, 0 hooks active" then drop to "Waiting for plugin" after the 60s grace expired. Probe now uses `GET /plugin/events` with `Accept: text/event-stream` (no JSON body), proving connectivity and auth without mutating registry state.
- **Fix(docker): eliminate dual plugin registration (jsonc tuple + plugins/chisl.mjs).** Entrypoint previously installed both an explicit `opencode.jsonc` plugin tuple AND a `plugins/chisl.mjs` auto-discovery loader on every start, risking OpenCode loading the plugin twice (duplicate hellos, duplicate SSE streams, registry race conditions). `prepare_chisl_plugin` now calls `ensure_chisl_plugin_in_config` first, then only installs the `plugins/*.mjs` loader if the jsonc has no Chisl reference (new `_jsonc_has_chisl_ref` helper). When the jsonc tuple exists, the loader is skipped with a log message.

### AionCore

- **Fix(plugin): config_snippet now points at `opencode-entry.mjs` not `dist/index.js`.** `services/remote.rs::plugin_install_info_impl` generated a config snippet with `file:///opt/chisl-opencode-plugin/dist/index.js` — the barrel file that re-exports ~30 symbols (many non-functions), causing OpenCode's legacy plugin loader to fail with "Plugin export is not a function". Updated to `file:///opt/chisl-opencode-plugin/opencode-entry.mjs` (thin default-export-only wrapper). Users copying the snippet from Chisl UI "Install Plugin" now get the correct path.
- **Fix(plugin): registry no longer lets probe hellos clobber real plugin state.** `PluginRegistry::record_hello` now detects probe hellos (`plugin_version == "0.0.0"` AND `hooks.is_empty()`) and only updates `last_hello_at_ms` for liveness — it does NOT overwrite `plugin_version`, `opencode_version`, or `hooks` if real values already exist. Defense-in-depth complementing the entrypoint probe fix; protects against any future probe or manual curl that sends minimal metadata.

### opencode-server-docker (earlier)

- **Fix(docker): use thin `opencode-entry.mjs` for Chisl plugin load.** OpenCode's legacy plugin loader iterates every export from the configured module and requires each to be a function; pointing at `dist/index.js` failed with "Plugin export is not a function" because the barrel re-exports helpers. Added `plugins/chisl-opencode-plugin/opencode-entry.mjs` (single default export), copied it into the image at `/opt/chisl-opencode-plugin/`, and rewired `entrypoint.sh`, `Dockerfile`, README, and `config/opencode.chisl-snippet.jsonc` to reference `file:///opt/chisl-opencode-plugin/opencode-entry.mjs`. Entrypoint also migrates existing configs from `dist/index.js` and refreshes the auto-loader stamp when the entry path changes.
- **Fix(docker): probe AionCore via `/plugin/hello` not `/global/health`.** Port 64921 is the AionCore plugin server — it has no OpenCode-only `/global/health` path. Entrypoint startup probe and README curl example now POST to `/plugin/hello` with the bearer token so reachability checks match the real plugin channel.
- **Fix(docker): load Chisl credentials from persistent `chisl.env` on the appdata share.** Unraid template variables added in a newer image often never reach an existing container until Edit → Apply → restart — and SSH login shells do not inherit Docker `-e` vars anyway, so `echo $AIONCORE_URL` inside SSH was a false negative. Entrypoint now loads `/config/.config/opencode/chisl.env` (host: `appdata/opencode/.config/opencode/chisl.env`), merges with container env (template wins when both set), syncs back when Docker env works, and installs `/etc/profile.d/chisl-env.sh` so interactive shells see the same credentials. Added `config/chisl.env.example` and README troubleshooting for PID 1 environ vs SSH.
- **Fix(docker): vendor Chisl plugin instead of broken AionUi sparse-clone.** GitHub Actions Docker CI failed because the Dockerfile tried to sparse-clone `iOfficeAI/AionUi` (404 — repo moved or is private). Fixed by committing `plugins/chisl-opencode-plugin/` into the opencode-server-docker build context, removing the git-clone fallback, and updating `scripts/vendor-chisl-plugin.sh` / `plugins/.gitignore` so CI builds from the vendored tree. Entrypoint and README updates document the bundled plugin loader and troubleshooting for "Waiting for plugin".

### AionUi

- **Fix(dev): `bun run dev` "Electron uninstall" regression — Bun skipped electron's postinstall.** Bun's package manager silently skipped or incompletely ran `electron@37.10.3`'s `install.js` postinstall, leaving `dist/Electron.app` partially extracted (missing `Frameworks/` and `path.txt`). `electron-vite` then threw "Electron uninstall" at dev start. Fixed by manually extracting the cached zip and writing `path.txt`. Added a durable `ensureElectronBinary()` guard to `scripts/postinstall.js` that runs on every `bun install`: verifies `path.txt` + macOS `Frameworks/` completeness, tries `electron/install.js`, falls back to cache extraction, and writes `path.txt` as last resort.
- **Feat(opencode-plugin): streaming-latency instrumentation for Phase 2 Metric 3 measurement.** Three timestamped measurement points added across the streaming shell path so the owner can diff server-emit vs UI-render timestamps during live QA. (1) Server-side: `tracing::trace!` with monotonic `ts_ms` and `chunk_seq` on every SSE chunk emit in `shell_stream.rs::forward_chunk` plus a `plugin_shell_done` trace at completion (trace-level, invisible at production `info`). (2) Plugin relay: `CHISL_SHELL_DEBUG=1` env gate enables `[shell:chunk_recv]` and `[shell:meta_emit]` console markers in `shell.ts`. (3) UI render: `import.meta.env.DEV`-gated `useEffect` in `MessageToolCall.tsx` logs `[ui:tool_render]` when tool output changes. Added `/// <reference types="vite/client" />` to renderer `types.d.ts` for `import.meta.env.DEV` type support.
- **Test(opencode-plugin): CI version-matrix job (Phase 2 Metric 5 proxy).** New `capabilityDetection.test.ts` (12 tests, 4 describe blocks) verifying: `chat.message` fallback injects context when `experimental.chat.system.transform` never fires; plugin doesn't crash when a hook is absent; `DECLARED_HOOKS` reported verbatim in hello payload; `detectServerVersion` degrades gracefully (returns `undefined`, never throws). New `plugin-version-matrix` CI job in `pr-checks.yml` runs the capability test + full opencode-plugin suite on every PR. True multi-version harness (booting real OpenCode servers) is infeasible headlessly; documented as a limitation.
- **Chore(tests): rebuilt better-sqlite3 native binding for Node 26 ABI.** The `better-sqlite3` native module was compiled for Bun's NODE_MODULE_VERSION 136 but vitest runs under Node.js v26 (MODULE_VERSION 147), causing 75 test failures across queue/indexer/approval schema and repository tests. Rebuilt via `node-gyp rebuild` — 75 tests recovered (133→58 failures, 19→12 failed files). Remaining 58 failures triaged in `Plans/phase-prompts/reports/Validation-Report.md`: 24 from missing `remoteAgent` IPC mock, 20 from stale layout expectations, 8 from stale TerminalPanelHost mock, 2 orphaned imports, 4 from incomplete test shims.
- **Feat(local-opencode): Phase 4 UI — "Start Local OpenCode" panel with instance management.** New `LocalOpenCodePanel.tsx` rendered above the remote agent grid in `RemoteAgentManagement.tsx`: start modal (optional name input), per-instance status badges (starting/running/stopped/crashed with semantic colors), stop/restart controls, 10s SWR polling for live status. New `localOpenCodeTypes.ts` (`LocalOpenCodeInstance`, `LocalOpenCodeStatus`, `StartLocalOpenCodeRequest`, `LocalOpenCodeListResponse`) and `localOpenCode` IPC bridge section (`list`/`start`/`stop`/`restart` via `httpGet`/`httpPost` to `/api/local-opencode/*`). 17 new `settings.localOpenCode.*` i18n keys translated across all 8 locales (en-US, zh-CN, ja-JP, zh-TW, ko-KR, tr-TR, ru-RU, uk-UA). `bunx tsc --noEmit` clean; `i18n:types` + `check-i18n.js` pass.

### AionCore

- **Feat(local-opencode): Phase 4 process manager for local `opencode serve` instances with auto-injected plugin config.** New module `aionui-ai-agent::manager::local_opencode` (4 files under `manager/local_opencode/`: `mod.rs`, `config.rs`, `instance.rs`, `manager.rs`) plus matching `routes/local_opencode.rs` and the new `LocalOpenCodeRouterState`. Each spawn runs `opencode serve` via `aionui_runtime::Builder` (inherits `kill_on_drop(true)` and the env-pollution stripper) with `OPENCODE_CONFIG_CONTENT={ "plugin": ["@chisl/chisl-opencode-plugin"], "env": { "AIONCORE_URL": ..., "AIONCORE_TOKEN": ... } }` and a per-instance `OPENCODE_DATA_DIR` so multiple locals don't share state. Port capture parses `http://…:PORT` from the child's stdout with a 30 s timeout; health check pings `/global/health` every 30 s in a background task; crash detection is driven by `Child::try_wait()`; restart is bounded at 3 restarts per rolling 5-minute window. New REST surface (all auth-gated): `GET /api/local-opencode` (list), `POST /api/local-opencode/start`, `POST /api/local-opencode/{id}/stop`, `POST /api/local-opencode/{id}/restart`. New types in `aionui-api-types::local_opencode`: `StartLocalOpenCodeRequest`, `LocalOpenCodeStatus` (Starting/Running/Stopped/Crashed, snake_case), `LocalOpenCodeInstance`, `LocalOpenCodeListResponse`. `AppServices` gained `host` + `port` fields (copied from `AppConfig`) so the manager can build the dial-back URL `{base}/plugin`; `commands/server.rs` graceful-shutdown path now calls `kill_all_local_opencode()` after `kill_all_bg_processes()` so a server restart doesn't leak any locally-spawned `opencode serve` children. 5 new unit tests (config JSON shape for http + https, port-line parser for `Server listening on http://…:PORT` / `Listening on …` / bare `http://…/`, restart-window bookkeeping). `cargo test -p aionui-ai-agent --lib` 802 passed; `cargo test -p aionui-api-types --lib` 476 passed; `cargo test -p aionui-app --lib` 27 passed; `cargo check` workspace clean.
- **Fix(events): ErrorEventData compile breakage in aionui-team + aionui-channel test targets.** The `ErrorEventData` struct gained `kind: Option<String>`, `metadata: Option<serde_json::Value>`, and `recoverable: Option<bool>` fields (all `#[serde(default)]`), but 6 test constructors across 3 files still used the old two-field shape (`message` + `code`). Added `kind: None, metadata: None, recoverable: None` to all 6 sites: `crash_detection.rs` (4 constructors in `tests` + `crash_tests` modules), `stream_relay_test.rs` (1), `message_service.rs` (1 lib test). `cargo check -p aionui-team --all-targets` and `cargo check -p aionui-channel --all-targets` now exit 0; `cargo test --workspace` compiles and runs (1 pre-existing e2e failure in `auxiliary_e2e.rs::slash_commands_no_active_task` remains — unrelated).
- **Fix(sidecar): 4 security-review findings on the reverse proxy.** (1) **Stream response body, don't buffer it (§8a MEDIUM).** `http_proxy` previously called `upstream.bytes().await` and stuffed the entire body into a `Vec<u8>`, which broke SSE/long-lived streams (response appeared hung until upstream closed) and created an unbounded-memory vector. Now uses `upstream.bytes_stream()` + `Body::from_stream(...)` so chunked/SSE responses flow through as they arrive. Module doc updated to reflect the new behavior. (2) **Non-loopback bind now warns at startup (§6 MEDIUM).** `commands/server.rs` checks the resolved `listener.local_addr()` after bind and emits a `tracing::warn!` if the IP is not loopback, explicitly calling out the sidecar proxy as part of the network-reachable surface. Default `--host 127.0.0.1` is unaffected; the warning only fires when the operator opted into a non-loopback bind. (3) **Connection-listed headers are now stripped (§3 LOW).** The request header copy loop now parses the inbound `Connection` header (comma-separated, case-insensitive) into a `HashSet<String>` and skips any header whose lowercased name appears in that set — closes the RFC 7230 §6.1 gap where `Connection: X-Secret` could otherwise smuggle app-internal headers past the hop-by-hop filter. The hop-by-hop list itself is unchanged. (4) **`Referer` is no longer forwarded to sidecar upstreams (§7 LOW).** The first navigation URL embeds the per-sidecar token as `?sct=<token>` and many sidecar upstreams log `Referer`, so the proxy now drops it on the way out. Two new regression tests: `sc18_connection_listed_headers_stripped` (sends `Connection: X-Secret, keep-alive` + `X-Secret: leaked-value` + `X-Normal: kept-value` and asserts the proxy returns 200 after applying the filter) and `sc19_referer_not_forwarded_to_upstream` (asserts the proxy returns 200 without crashing when the Referer embeds a token). 20/20 `router::sidecar` lib tests green; `cargo check -p aionui-app --all-targets` clean.

## 2026-06-10

### AionUi

- **Feat(opencode-plugin): Chisl OpenCode Plugin Bridge MVP — first-party plugin package (control plane).** New workspace package `packages/opencode-plugin` (`@chisl/chisl-opencode-plugin@0.1.0`, publishable, sole dep `@opencode-ai/plugin@1.16.2`): runs inside a remote OpenCode server and dials back to AionCore over a bearer-authed, fetch-based SSE channel (standard `EventSource` cannot send `Authorization` headers) with exponential-backoff reconnect + re-hello. Carries exactly the control-plane split from the framework doc: context injection (prefers `experimental.chat.system.transform`; degrades to a synthetic `chat.message` TextPart via a runtime latch that self-disables the fallback once transform fires), tool-audit forwarding (`tool.execute.before/after` carrying the real `sessionID` — closes the OpenCode 1.15/1.16 MCP gap where `x-opencode-session-id` headers are never sent on remote MCP HTTP calls), `permission.ask` routing (decision applied from AionCore; on network failure `output.status` is left untouched so the native flow proceeds), event forwarding filtered to `file.watcher.updated`/`session.idle`/`message.part.updated`, and a `run_shell_streaming` tool that forwards to AionCore and surfaces chunks via throttled `ctx.metadata()`. Every hook body is try/catch-wrapped — the plugin can never crash the host; it deliberately duplicates zero MCP data-plane tools. Post-review hardening: removed a `globalThis` client shim that left the shell tool permanently "not configured" in production (now a `createRunShellStreamingTool(getClient)` factory over a boxed client ref, cleared on `dispose`), and added a public `AionCoreClient.openShellStream()` instead of a private-field reach-in. 54 tests in `tests/unit/opencode-plugin/` (SSE parser chunk-split cases, backoff, context latch, audit payloads, permission failure semantics, shell streaming/abort).
- **Feat(settings): one-click "Install Plugin" snippet generator with live verify for remote OpenCode agents.** New `AgentSettings/PluginInstall/` modal, launched from a new button on opencode-protocol agent cards: shows the generated `opencode.json` plugin entry + `AIONCORE_URL`/`AIONCORE_TOKEN` env snippet (token masked by default, show/hide toggle), per-setup instruction tabs (bare server / Docker / systemd, each with copy-pasteable blocks), a live verify-installation banner polling `GET /api/remote-agents/{id}/plugin` every 5s (turns green with plugin version, OpenCode version, and active hook count once the plugin's `/plugin/hello` lands), and rotate-token with a destructive-action confirm. New bridge methods `remoteAgent.getPluginInfo`/`rotatePluginToken` + `RemoteAgentPluginInfo`/`RemoteAgentPluginStatus` types. 29 new `settings.remoteAgent.plugin.*` keys translated in all 8 locales.
- **Docs(opencode-plugin):** new `docs/opencode-plugin/install-guide.md` (user walkthrough incl. tunnels/containers via `AIONUI_LOCAL_FS_MCP_PUBLIC_URL`, troubleshooting matrix) and `developer-guide.md` (module map, capability-detection contract with `DECLARED_HOOKS`, never-crash rule, security posture, SDK version-matrix bump policy keyed on `opencode-sdk-version.json`).

### AionCore

- **Feat(remote): plugin webserver — the control-plane channel for the Chisl OpenCode plugin.** New module `manager/remote/plugin/` (`protocol`/`registry`/`server`/`shell_stream`) alongside — explicitly not replacing — `local_fs_mcp`. A process-singleton axum server (bound via the shared `reachability::plan`, same remote→local dial-back direction and URL-candidate machinery as the MCP bridge) exposes protocol v1: `POST /plugin/hello` (records plugin version, OpenCode server version, and declared hooks per agent — feeds the UI verify indicator), `GET /plugin/events` (authed SSE push channel; Drop-guarded `events_stream_open` flag; broadcast lag now emits a `lagged` event and continues instead of killing the stream), `POST /plugin/result` (toolBefore/toolAfter/event audit records with real OpenCode session ids into a 500-entry in-memory ring buffer — raw args/output never reach logs, previews capped at 2048 chars; permissionAsk answers passthrough `"ask"` in the MVP), and `POST /tools/run_shell_streaming` (SSE `chunk`/`done`/`error`; gated by the conversation's `ShellApprover` — registered from `ensure_local_fs_mcp`, unregistered on teardown, no approver → deny; spawned via `aionui_runtime::Builder::clean_cli`; 120s default / 1h max timeout, 4 MiB stream cap, child killed on timeout **and** on client disconnect via a `tx.closed()` select branch — review fix for silent-command orphan processes). Per-workspace bearer `plugin_token` (UUIDv4; stored plaintext by documented choice — the AES-GCM auth_token scheme uses a random nonce, making SQL-equality lookup impossible; migration `010_remote_agent_plugin_token.sql`) is resolved from the DB on every request, so rotation invalidates new requests immediately. New REST surface for AionUi: `GET /api/remote-agents/{id}/plugin` (endpoint URL + candidates from reachability, token, config/env snippets, live status incl. audit count) and `POST .../plugin/rotate-token`. Post-review hardening: removed a tautological constant-time self-compare and corrected PROTOCOL.md to state the truthful auth posture (~122-bit token entropy + SQL equality; default bind `0.0.0.0` with the bearer token as the sole gate). `PROTOCOL.md` gained a "Chisl Plugin Channel (protocol v1)" section documenting all wire shapes. 37 new tests across registry/server/shell_stream plus db token-roundtrip and service snippet/install-info tests; `cargo test -p aionui-db -p aionui-ai-agent` fully green (1281 passed).

## 2026-06-09

### AionUi

- **Feat(terminal): A2 — flood-safe output, reload re-attach, and a minimal split for the integrated terminal.** The terminal panel (xterm.js 5.5 + main-process `@lydell/node-pty`, both pre-existing) had three structural gaps closed: (1) **Flood safety** — `TerminalService` no longer emits one IPC event + one renderer `term.write` per PTY chunk; output now coalesces per session (8ms timer / 64KB cap, flushed before exit so nothing is lost) and the renderer writes through a new single-flight `writeQueue` (chained `term.write(data, cb)`, 1MB-per-write cap). Measured in `terminalServiceFlood.test.ts`: 10,000 chunks (640KB) → **10 events (1000× coalescing), lossless**. (2) **Reload survival / orphan-PTY fix** — new `terminal.list` + `terminal.snapshot` IPC (registered in the adapter allowlist) backed by a 512KB tail ring buffer per session; on renderer mount, live PTYs are re-listed as `restored` tabs and their scrollback replayed with a race-free subscribe-buffer-replay-drain ordering — previously a renderer reload silently orphaned running shells until app quit. (3) **Minimal split** — a Split button renders two side-by-side panes via `react-resizable-panels` (right pane = fresh session with the active tab's cwd, closable; per-pane tab groups intentionally deferred), i18n'd (`terminal.split.*` × 8 locales). Emulator decision documented in `docs/qa/A2-terminal.md`: ghostty-web rejected on upstream showstoppers (WASM memory corruption on dispose coder/ghostty-web#141, viewport data corruption #139, broken paste/scroll #145/#148, 6-month-stale release) in favor of the already-integrated xterm.js. +25 tests (9 flood/ring/list, 9 writeQueue, 7 re-attach/split).
- **Feat(settings): A5 Connect Wizard — guided first-run flow for registering a remote OpenCode server.** New 3-stage wizard (`components/settings/ConnectWizard/`): form (URL, auth, insecure-TLS toggle, tool host) → live connect checklist (test connection → save agent → handshake → fetch models, each with status icons) → done (agent summary + "Start chatting"). Auto-opens on the New Chat page when the user has zero remote agents and hasn't skipped it (`chisl.connectWizard.dismissed` in localStorage; plain close does NOT persist, only Skip does); relaunchable from Settings → Agents via a new "Setup wizard" button. On success the agent becomes the default (if none) and is pre-selected on New Chat — one message away from a live conversation. Failures show classified, actionable messages (see next entry); a TLS failure with insecure-TLS off offers a one-click "retry with insecure allowed". Reuses the existing testConnection/create/handshake/refreshModels plumbing — no new backend surface. 23 new `settings.connectWizard.*` keys × 8 locales; 6 tests in `tests/unit/renderer/settings/connectWizard.dom.test.tsx` (happy path order-of-calls, auth-failure message, TLS retry sets allow_insecure, handshake retry doesn't duplicate create, skip persistence semantics, state roundtrip).
- **Feat(remote): A5 classified connection errors — distinct, actionable failure messages.** AionCore now embeds a stable `[code:<x>]` marker in connect-failure strings (see AionCore entry); new renderer util `utils/remote/connectError.ts` parses it (with text heuristics as fallback for older cores) and maps to localized messages with suggested fixes under `settings.connectError.*` (9 codes: dns_failure, tls_failure, auth_failure, not_opencode, connection_refused, timeout, server_error, workspace_not_on_server, unreachable) × 8 locales. Wired into the Remote Agent save/handshake flow and the Connect Wizard. 29 tests in `tests/unit/renderer/remote/connectError.test.ts`, including a locale-drift guard asserting every code has an en-US message.
- **Feat(conversation): A3 reverted-session inactive region + revert-point recording.** "Revert to here" now records `extra.revert_message_id` alongside `is_reverted` (and unrevert clears both), so the UI knows WHERE the session was reverted, not just that it was. `MessageList` dims every message from the revert point onward (`message-item--inactive`, semantic tokens, text still selectable) and renders a single labeled divider ("Reverted — messages below are no longer part of the session", `role="separator"`) before the first inactive message — covering composite items (tool/file summaries) via their source-message ids. Falls back to badge-only (no dimming) when `revert_message_id` is missing/unknown (older sessions). The conversation's `extra` bag now flows through `ConversationContext` (RemoteChat/ChatConversation/TeamChatView forward it). Also localized the full session-actions menu + reverted badge and the previously-defaultValue-only revert/fork strings across all 8 locales. 13 tests in `tests/unit/renderer/conversation/remoteSessionRevert.dom.test.tsx`: revert/unrevert round-trips asserted against the IPC boundary (`revertRemoteSession`/`unrevertRemoteSession` + extra updates), fork-from-message round-trip (sessionKey/history_loaded/preserve_session_key + navigation), exhaustive inactive-region helper coverage, applied-render proofs, and affordance gating (remote vs local).
- **Feat(preview): A4 one-click "View diff" on tool-call messages + working click-to-jump from that path.** Every tool message that modified files (Edit/replace tool calls, ACP diff blocks, grouped file changes) now has an explicit "View diff" button per file row (real `<button>`, icon + i18n label `preview.viewDiff` × 8 locales) opening the Pierre `DiffView` in the Preview panel — previously the only affordance was the subtle clickable +N/−M stats. Fixed the path plumbing so click-to-jump works from this entry point: `useDiffPreviewHandlers.handleDiffClick` (and `MessageFileChanges`) now propagate the file path, and `usePreviewLauncher` skips the disk-read branch for diff previews (a diff is a synthetic comparison, not a file) while still resolving `metadata.file_path` to the absolute on-disk path — so a line click in the diff fires `requestEditorRevealLine` + `shell.openFile({file_path, line_number})` with a real path instead of undefined. 7 tests in `tests/unit/renderer/conversation/toolMessageDiffLink.dom.test.tsx`. QA docs: `docs/qa/A4-diff-loop.md`, `docs/qa/A5-connect-wizard.md`.
- **Docs(qa): A1 live-pass failure recorded — server-tools-mode permission replies were not directory-scoped.** A live A1 pass in server-tools mode (OpenCode at `192.168.0.5:4096`, conversation `74ceeacb`) hung: the UI→AionCore confirm returned 200 but AionCore's reply to OpenCode 404'd with `PermissionNotFoundError` because the reply URL lacked the `?directory=<workspace>` scoping that routes it to the per-directory app instance holding the permission. Updated `docs/qa/A1-permission-question-relay.md`: new matrix rows (server-tools permission/question/timeout scoping — live FAIL → fixed; canonical-404 fallback; double-404 re-queue), the directory-scoping requirement documented in trace step 12, and a new "Mock-proven vs live-proven" section (the original 8 wiremock tests used `tool_host: "local"` and pinned AionCore's belief, not the server's contract). Also updated `Plans/phase-prompts/reports/Phase-1-Report.md`: A1 status downgraded from "Complete & green" to "Fixed after live-pass failure; pending live re-verification", with the hotfix change list and the workspace-502 investigation findings (server-tools conversations advertise a client-local/temp workspace path that is never validated against the server's filesystem — filed as an A5-adjacent follow-up).

- **Chore(opencode): single source of truth for the pinned `@opencode-ai/sdk` version.** The AionUi TS sync script and the AionCore Rust conformance suite each maintained their own version pin (`1.16.2` in `package.json`, `1.15.11` in `PROTOCOL.md` last-verified line), so the two repos could drift silently. Added `<chisl-root>/opencode-sdk-version.json` as the SoT and wired both sides to read it. AionUi's `scripts/sync-opencode-types.js` now reads the same JSON, refuses to regenerate with a stale `package.json` or installed SDK, surfaces a `Bump the JSON, package.json devDependencies.@opencode-ai/sdk, and the installed SDK together.` error, and got a `--check` mode (wired into the CI conformance workflow + a new `bun run types:sync-opencode:check` script) that exits 2 on drift. New `package.json` script `types:sync-opencode:check` exposes the check mode to local devs. The generated `opencodeProviderTypes.ts` now embeds a `// Pinned at generation: @opencode-ai/sdk@1.16.2 (matches ../opencode-sdk-version.json).` line so a code-search for the SDK version lands on the SoT path.
- **Docs(legal): third-party attributions for bundled OSS (Monaco, Pierre, etc.).** Added `THIRD_PARTY_NOTICES.md` at repo root and under `public/` (served in dev/build and copied into the Electron bundle via `electron-builder.yml` alongside `LICENSE` / `NOTICE`). Extended `NOTICE` and `readme.md` Attribution to point at the notices file. Settings → About → **Third-party notices** opens a modal that loads the markdown (`ThirdPartyNoticesModal.tsx`). Fixed Vite `publicDir` to resolve repo-root `public/` so pet assets and notices share one directory. i18n keys `settings.thirdPartyNotices*` in all 8 locales.
- **Refactor(media): replace `diff2html` with `@pierre/diffs` + `@pierre/theme`.** The two diff renderers (`Diff2Html.tsx` for inline chat previews, `DiffViewer.tsx` for the full Preview panel) routed through `diff2html`'s HTML-string injection, which clashed with our Shadow-DOM-strict design and made theming an after-the-fact `d2h-` class override. Reworked both to delegate to a new shared `DiffView` component (Pierre's `PatchDiff` wrapped in a `WorkerPoolContextProvider` for off-thread Shiki highlighting + lazy/virtualized rendering). Features: stacked/side-by-side toggle (`diffStyle: 'unified' | 'split'`), file-path resolution (explicit `file_path` prop → first filename from `parsePatchFiles`), and click-to-jump: a line click fires `requestEditorRevealLine({ line })` AND `ipcBridge.shell.openFile({ file_path, line_number })` so both the in-app Monaco editor and any OS-default-app path get a chance to react. To support the line-number payload without breaking existing call sites, `shell.openFile` is now typed as `{ file_path: string, line_number?: number }` (`httpPost` body forwards both fields; backend currently ignores `line_number` for forward-compat) — the 5 pre-existing string callers (`useWorkspaceFileOps` x2, `PDFViewer`, `PreviewPanel`, `UpdateModal`) were updated to the new object form. Custom Chisl theme: a new `chislTheme.ts` registers two Shiki `ThemeRegistration` JSONs (`chisl-light`, `chisl-dark`) — parchment backgrounds (`#f6ecc8` / `#1f1c17`), signature rust accents (`#b4480c` / `#e07820`), olive/cream/gold/teal syntax colors — via `registerCustomTheme`, paired via `theme={{ light, dark }}` + `themeType: 'light' | 'dark'` driven by the existing `data-theme` MutationObserver, plus an `unsafeCSS` layer that maps `data-diffs-line-type="addition|deletion"` to Chisl `--success` / `--danger` via `color-mix` at 18-22% alpha for printed-ink highlights. Theme registration is idempotent (module-local `Set` guard) so React 19 strict-mode double-invoke and HMR don't crash. Build wiring: dropped `diff2html` from `package.json`, added `@pierre/diffs@^1.2.8` + `@pierre/theme@^1.0.3` (kept `diff` as an explicit dep because `MessageToolCall.tsx` and `MessageAcpToolCall.tsx` still call `createTwoFilesPatch` to _produce_ unified-diff strings for `DiffView` — Pierre consumes `diff` transitively for parsing only, not for generation), swapped the Vite `vendor-diff` manual chunk for `vendor-pierre`, removed `diff2html` from `optimizeDeps.include`, added `@pierre/diffs` and `@pierre/diffs/react` to pre-bundling. Deleted the now-unused `Diff2Html.css` (35 selectors, all `d2h-`-prefixed). `bunx tsc --noEmit` clean; oxlint 0 errors (805 pre-existing warnings unchanged); `oxfmt --check` clean on all touched files. Pre-existing 109 test failures confirmed unchanged on baseline; no new test regressions introduced.
- **Feat(conversation): visual indicator for reverted remote sessions.** The session state was unclear after reverting a remote OpenCode session because messages were removed without leaving a visible "Reverted" state, and the "Restore reverted" menu item was always visible. Added an `is_reverted` flag to the remote conversation `extra` state, which optimisticially updates on successful `revertRemoteSession` and `unrevertRemoteSession` IPC calls. `RemoteSessionBadge` now reads this flag to display an orange "Reverted" tag with a descriptive tooltip. `RemoteSessionActions` now uses this flag to conditionally hide the "Restore reverted" menu item unless the session is actually in a reverted state.
- Added vertical resize handles to Sider workspace panel sections, allowing users to drag and adjust the height of each expanded section.
- **Feat(sider): drag-and-drop sortable workspace sections.** The 4 sections (Explorer, Diff, Outline, Timeline) in `SiderWorkspacePanel` can now be reordered by dragging their headers, using `@dnd-kit/sortable`. The user's customized section order is persisted in `localStorage` (`sider.section.order`) so it survives app restarts. Visual feedback (dimming, shadow) is provided while dragging.
- Simplified Remote Agent Connect Wizard flow by merging 'Test Connection' into the 'Save' button. This prevents saving broken configurations and immediately runs both a connection test and handshake. Added automatic model caching and default agent selection to streamline the first-message experience.
- **Docs/Test(remote): A1 permission + question relay — proved the "no-op resolve" defect does not exist and pinned server acknowledgment.** Audited the full OpenCode approval round-trip and found the suspected `pendingPermissions` / `(_response) => {}` no-op is **stale documentation** describing a removed OpenClaw WebSocket path — those symbols exist in zero source files. The live path is `permission.asked` SSE → AionCore → `acp_permission` emit → renderer card → `httpPost /api/conversations/{id}/confirmations/{call_id}/confirm` → AionCore `RemoteAgentManager::confirm` → real `POST /permission/{id}/reply` to OpenCode (questions: `/question/{id}/reply`). Corrected `docs/prds/conversations/remote/remote-agent.md` F-RAGENT-11 (real SSE→HTTP mechanism, removed the unverified "已修复" prose) and fixed a 70s→**60s** timeout inconsistency (`DEFAULT_PROMPT_TIMEOUT_MS = 60_000`; one acceptance row had said 70s). Added `docs/qa/A1-permission-question-relay.md` with the per-hop trace + a QA matrix where every cell records **server acknowledgment** (not just UI state). Added `tests/unit/renderer/conversation/useWorkspaceApprovals.dom.test.ts` (5 tests): remote prompt surfaces in the Approvals tab, turn-boundary `finish`/`error` auto-clear, MCP-elicitation stays inline (no regression), cross-conversation filtering.

### AionCore

- **Feat(remote): A5 classified connection-test failures + server-tools workspace fail-fast.** (1) `test_opencode_health` (`services/remote.rs`) now embeds a stable `[code:<x>]` marker in every failure message so the renderer can show actionable localized errors instead of one opaque BadGateway string: network errors are classified via a new pure `connect_error_code_from_text` over the flattened reqwest `source()` chain (`dns_failure`/`tls_failure`/`connection_refused`/`timeout`/`unreachable`; DNS checked before TLS so a DNS chain mentioning certificates reports the root cause), HTTP 401/403 → `auth_failure`, 404 → `not_opencode` (the endpoint has no `/global/health` — likely not an OpenCode server), 5xx → `server_error`, non-JSON health body → `not_opencode`, `healthy:false` → `server_error`. (2) The A1 live-pass follow-up: `opencode_create_session` in server-tools mode now probes the workspace on the remote server first (`opencode_fs::fetch_path`, i.e. `GET /path?directory=<workspace>`) and fails session creation with `[code:workspace_not_on_server]` instead of letting the conversation limp along with `GET /workspace` 502s and spurious `external_directory` permissions when the path doesn't exist server-side; local tool-host mode is untouched (no probe). PROTOCOL.md documents the probe contract. 16 new tests: 5 pure-classifier, 8 health-classification (wiremock 401/403/404/500/non-JSON/unhealthy + a real dropped-listener connection-refused + a live `.invalid`-host DNS case), and 3 session-create tests (server-mode probe-then-create happy path; probe-404 fails fast with `POST /session` asserted never sent via `.expect(0)`; local mode sends no `/path` request). `cargo test -p aionui-ai-agent --lib remote::` → 272 passed.
- **Fix(remote): directory-scope OpenCode permission/question replies in server-tools mode + fallback + loud failure (A1 hotfix).** Root cause of the live-pass hang: `build_permission_reply_request` (and the question reply/reject builders) produced unscoped URLs while sessions in server-tools mode live in OpenCode's per-directory app instance (`?directory=<workspace>`), so replies hit the default instance's empty permission registry → 404 `PermissionNotFoundError` → tool call parked forever, card already cleared. Changes in `crates/aionui-ai-agent/src/manager/remote/`: (1) all permission/question reply, reject, timeout-reject, and cancel-reject URLs now carry the directory param via new `opencode_context::append_v1_directory_value` (no-op in local tool-host mode — behavior unchanged there); (2) new `post_permission_reply_with_fallback` retries once via the deprecated-but-served session-scoped `POST /session/{sid}/permissions/{pid}` body `{"response":…}` (new `OpencodePermissionRespond` payload) when the canonical endpoint returns non-2xx, logging both attempts; (3) if both endpoints fail, the confirmation card is re-queued (once per id, tracked in new `RemoteState.requeued_permissions` to prevent approve→re-queue ping-pong on genuinely-resolved permissions) and the dedup stamp cleared so the user can retry — no more silent hang; failed question replies clear the question dedup stamp so a server re-emit can resurface the cards. (4) +5 wiremock regression tests with strict `?directory=` query matchers (`server_mode_permission_reply_is_directory_scoped`, `server_mode_question_reply_is_directory_scoped`, `server_mode_timeout_sweep_reject_is_directory_scoped`, `permission_reply_404_falls_back_to_session_scoped_endpoint`, `permission_reply_double_404_requeues_confirmation`) plus a parametrized `opencode_agent_with(url, tool_host, workspace)` test helper; 77 `remote::agent` lib tests green, fmt/clippy clean. `PROTOCOL.md` updated: the canonical reply endpoint now documents the directory-scoping requirement and the session-scoped alias is documented as the one-shot fallback (previously "intentionally avoided").
- **Test(remote): A1 server-acknowledgment tests for the permission + question relay.** The existing `remote::agent` tests asserted only in-memory state clearing on confirm/sweep — they never proved the user's decision actually reaches OpenCode (the Phase-1 win-condition metric). Added 8 `#[tokio::test]`s in `crates/aionui-ai-agent/src/manager/remote/agent.rs` that stand up a `wiremock` OpenCode server, point a real `RemoteAgentManager` at it, drive a confirm/sweep, and assert the canonical reply POST lands on the wire with the correct body: allow-once/always/reject (`{"reply":"once|always|reject"}`), `allow_dir` degrading to `once`, two concurrent prompts each acked independently, a question reply (`POST /question/{id}/reply {"answers":[["Postgres"]]}`), and the 60s timeout sweep posting `{"reply":"reject"}`. Plus a guard test pinning `DEFAULT_PROMPT_TIMEOUT_MS == 60_000` (the implementation was always 60s; legacy prose said 70s). All 72 `remote::agent` lib tests green.
- **Chore(opencode): single source of truth for the pinned `@opencode-ai/sdk` version.** Companion to the AionUi entry above. The conformance crate's `PROTOCOL.md` last-verified line (`1.15.11`) and the AionUi `package.json` devDep (`1.16.2`) had drifted; both now read from `<chisl-root>/opencode-sdk-version.json`. The `aionui-opencode-conformance` crate gained a `build.rs` that parses the JSON at build time and emits a `version.rs` with `pub const OPENCODE_SDK_VERSION: &str`, `pub const OPENCODE_SDK_PACKAGE: &str`, `pub const OPENCODE_BINARY_VERSION: &str`, and `pub const OPENCODE_SDK_PIN_LAST_VERIFIED: &str` into `OUT_DIR`, re-included from `lib.rs` via `include!(concat!(env!("OUT_DIR"), "/version.rs"))`. Added a new `[build-dependencies] serde_json` entry to the crate's `Cargo.toml` and a `build = "build.rs"` package key. Three new lib tests guard the contract: `sdk_version_pin_is_populated` (asserts the constant is a non-empty semver), `sdk_version_pin_matches_package_name` (asserts `OPENCODE_SDK_PACKAGE == "@opencode-ai/sdk"`), and `sdk_version_pin_matches_package_json_dev_dependency` (asserts the JSON `version` matches `AionUi/package.json` devDependencies.`"@opencode-ai/sdk"` — a deliberate bump-the-two-sides-together test; the assertion message names the SoT path so the next bump has zero guesswork). `PROTOCOL.md` got a new "Current pin" line at the top pointing at the JSON and a "Version pin contract" section with a 5-step bump procedure. The conformance crate got a `README.md` pointing at the SoT. `.github/workflows/conformance.yml` gained a `Verify SDK pin file is present` pre-step (hard-fails the job with a clear error pointing at the contract instead of letting `build.rs` panic) and a `Run AionUi types-sync pin check` post-step that runs `node scripts/sync-opencode-types.js --check` so both sides of the contract gate a PR. `cargo test -p aionui-opencode-conformance --all-targets` 21/21 green (16 lib + 5 integration); a manually-mutated pin in the JSON trips a precise "Bump them together" assertion failure in both the Rust test and the TS `--check` script.

## 2026-06-08

### AionUi

- **Feat(sider): merge Local History into the Timeline, add click-to-diff + Restore.** The new Sider Timeline was git-only (`ipcBridge.git.getFileLog`), so a fresh project with no commits or a user who hasn't initialized git saw an empty panel even though the new `LocalHistoryService` had already snapshotted the file (on open / save / agent write). Reworked `SiderTimelineSection.tsx` to fetch BOTH sources in parallel via `Promise.all` (`git.getFileLog` + `localHistory.listEntries`), tag each row with a `kind: 'git' | 'local'` discriminator, and sort the merged list newest-first (git `entry.date` epoch vs local `entry.timestamp`). Local rows render as `Local History` + a colored `Tag` badge (Save / Agent Edit / Restore Point / Initial Open / Auto-Save — i18n'd via `conversation.sider.timelineLocal{Source}`) with `Local` in place of the author hash. Every row is now keyboard- and clickable (real `<button>`-style `role=button`/`tabIndex`/`Enter`+`Space` handler, focus-visible brand outline, hover `bg-2`) and opens a new `TimelineDiffModal` portal-based overlay. The modal mounts a Monaco `DiffEditor` (left = historical content, right = active buffer) via `monaco.editor.createDiffEditor` with two scratch models (unique `aionui-timeline-*://` URIs so a model can live in one editor) and disposes both on close/file-change/unmount — the same prior art as `MonacoEditor.tsx:686`. Language is resolved from `activeBuffer.language` first, then the file extension via `monaco.languages.getLanguages()`. Restore flow: (1) `localHistory.addSnapshot` with `source: 'restore'` to checkpoint the pre-restore state, (2) `fs.writeFile` to persist the historical content, (3) `editor.applyExternalContent(activeBuffer.key, oldContent)` to update `content`+`originalContent` (clears the dirty flag) — the existing Monaco auto-sync effect (`MonacoEditor.tsx:638`) then pushes `oldContent` into the live model via `pushEditOperations`, so the editor updates without a reload. Modal also has a danger "Delete" for local entries and shows an `Empty` state for git rows (we'd need a `git show <hash>:<path>` IPC to fetch historical git content, deferred). The Timeline SWR `mutate()` is called after restore/delete so the list refreshes. Added 10 new i18n keys (`timelineLocalSubject`, `timelineLocalMeta`, `timelineLocalSave`, `timelineLocalAgent`, `timelineLocalRestore`, `timelineLocalOpen`, `timelineLocalAutosave`, `timelineRestoreBtn`, `timelineGitTitle`, `timelineGitUnsupported`) across all 8 locales. `bunx tsc --noEmit` clean; `bunx oxlint` 0 warnings/errors on the new/modified files; `bun run i18n:types` + `node scripts/check-i18n.js` pass.
- **Feat(sider): move the editor Outline into a VS Code-style collapsible left-Sider, add a file Timeline.** The Outline lived as a left rail _inside_ the code editor (`EditorPanel` → `EditorOutline`, gated by a View-menu toggle), which split file structure away from the Explorer/Diff already in the Sider. Reworked the Sider workspace column (`SiderWorkspacePanel.tsx`) from a 2-pane `react-resizable-panels` split into a hand-rolled VS Code-style accordion of four collapsible sections — Explorer, Diff, Outline, Timeline — each persisting its open/closed state to `localStorage` (`sider.section.*`; Explorer+Diff default open, Outline+Timeline default collapsed). New `SiderAccordionSection` provides the reusable collapsible chrome (real `<button>` toggle, `aria-expanded`/`aria-controls`, chevron, flex body so open sections share vertical space). New `SiderOutlineSection` (`React.memo` + `useEditorContext`, keyed `useMemo` on `language`+`content` so typing only re-renders this section, never the file tree/diff) reuses the existing `extractOutline`/`glyphFor` parser and the `.editor-outline*` CSS; clicking a symbol dispatches a new `editor.reveal.line` window `CustomEvent` (added to `editorReveal.ts`, mirroring the existing `editor.reveal.path` tree handoff) which `EditorPanel` now listens for and forwards to the focused Monaco group's `revealLine`. Removed the outline `<aside>`, the `outlineVisible` state/persistence, and the View-menu outline toggle from the editor (Problems stays); deleted the now-unused `EditorOutline.tsx`. `SiderFileTree`/`SiderDiffSection` gained an optional `headerless` prop so the accordion owns their header. Added 7 `conversation.sider.*` i18n keys across all 8 locales. `bunx tsc --noEmit` clean; oxlint 0 warnings/errors on new files; `check-i18n.js` passes (types in sync).
- **Feat(git): add `git.getFileLog` IPC for single-file commit history (Timeline data source).** No git-log surface existed. Added a path-limited `GitService.getFileLog({ workspace, file_path, max_count })` (default 50, capped 200) using `simple-git` `git log --max-count -- <file>` with an ASCII unit/record-separator `--pretty` format so commit subjects parse unambiguously; returns `GitFileLogEntry[]` (`hash`, `shortHash`, `subject`, `author`, ISO `date`), newest-first, and an empty array for non-repos / unborn repos / untracked files (never throws). Wired end-to-end: new types in `gitTypes.ts`, provider in `gitBridge.ts`, `ipcBridge.git.getFileLog` (`git.file-log` channel), and the `events.ts` request/response/allowlist entries. Consumed by `SiderTimelineSection` (SWR keyed on the active buffer's file path with a 300ms debounce to avoid a git-invoke burst when scrubbing tabs).
- **Feat(ui): implement agent provenance, structured shell runs, and mode selector i18n.** Executed a UI consistency and feature pass across the renderer. (1) Added an "Agent-modified" provenance badge to the Git Changes list (`GitChangeList.tsx`). The badge appears next to files edited by the agent in the current conversation, derived by extracting paths from `acp_tool_call` (edit/write) and `tool_group` messages via a new `extractAgentEditedPaths` helper in `normalizeToolCall.ts`. (2) Upgraded ACP shell-command tool calls (`MessageAcpToolCall.tsx`) to render as structured pass/fail blocks. Replaced the local gray-only `StatusTag` with the shared `StatusPill`, surfaced the bash exit code as a color-coded pass/fail indicator, rendered the command string legibly as a shell line, and i18n'd the kind labels. (3) Tokenized hardcoded diff colors in `Diff2Html.css` using `color-mix` with `--success` and `--danger` semantic tokens. (4) Cleaned up the layout mode selector: replaced hardcoded English strings in the `Titlebar` pill-slider with i18n keys and deleted the unused `LayoutModeSelector` stub directory. Added 6 new i18n keys across all 8 locales. Added 22 unit tests covering the pure derivation logic for provenance paths and pill state.
- **Feat(layout): reorder Command Center to file/context → editor → chat → session details (desktop).** The code editor was a peer pane mounted _inside_ `ChatLayout` to the right of chat (and a separate `EditorPane` did the same on `/guid`), so the spatial flow read files → chat → editor → chat-details, which crossed the editor to reach the chat/session pane. Extracted the editor shell into a single `CommandCenterEditorHost` and hoisted it to the app shell (`Layout.tsx`) as a peer column between the Sider and the chat content, so in command-center mode the editor now sits to the LEFT of chat on every route that has one (conversation routes + `/guid`/settings). Team routes stay editor-less (they never used `ChatLayout`, and their route `:id` is a team id, not a conversation id). Retired the duplicate `EditorPane.tsx` so there is exactly one editor instance app-wide. The host self-resolves its `workspaceRoot` from the route `:id` via `ipcBridge.conversation.get` (mirrors `SiderFileTree`), preserving editor tab-restore. Editor open/collapse/blade behavior is owned by `EditorContext` and unchanged — this was a positional move, not a logic change; chat-mode layout and the chat view itself are untouched.
- **Feat(editor): operator-selectable editor dock side (left/right of chat).** New `useEditorDock` store (`utils/layout/editorDock.ts`, persisted to `aionui.commandCenter.editorDock`, default `start` = left of chat; synced across all consumers via a window event + `useSyncExternalStore`, no provider). A single arrow-only toggle in the editor toolbar flips it: docked left shows `→` (move right of chat), docked right shows `←` (move left). The swap is done purely via CSS flex `order` (Sider 0, editor/chat 1↔2, ConversationPane pinned 3) so the editor never reparents and Monaco never remounts — tabs/cursor survive the toggle. The pane seam border, resize-handle edge (+`reverse` direction), and collapsed-blade chevron all follow the dock side via `.editor-pane--dock-start` / `--dock-end`. New i18n keys `conversation.editor.dockToRight` / `dockToLeft` (all 8 locales).
- **Fix(layout): editor pane no longer cramps/clips its neighbors and is freely resizable.** Hoisting the editor to the shell exposed two bugs. (1) The pane was `flex-shrink: 0` with a hard `min-width: 360px`, so on ~≤1380px windows the fixed Sider + editor + ConversationPane plus chat's 360px (`--app-min-width`) floor overflowed and clipped the rightmost pane. Fixed by making the expanded editor `flex-shrink: 1` with `min-width: 0` so it yields space under viewport pressure (the 44px blade and closed states stay rigid), and relaxing the operator drag range from 360–960px to 120–2000px. (2) The resize grip was still wired for the old right-docked position (left edge + `reverse: true`), so it overlapped the Sider's own handle and dragged the wrong way — moved to the editor's chat-facing edge, which now follows the dock side. The `chat-editor-width-px` width preference is unchanged.
- **Test(layout): cover the Command Center editor host + dock store.** `tests/unit/renderer/commandCenterEditorHost.dom.test.tsx` (layout-mode gating renders null off command-center; dock-driven `order` / seam class / resize-handle edge; no-remount on dock toggle via a mount counter) and `tests/unit/renderer/editorDock.dom.test.ts` (default, localStorage roundtrip, invalid-value fallback, `toggleDock`, cross-consumer event sync) — 13 tests. `bunx tsc --noEmit` clean; `node scripts/check-i18n.js` passes (types in sync, 8 locales consistent).
- **Feat(editor): "Send Selection to Chat" right-click action.** Registered a Monaco editor action (`aionui.sendSelectionToChat`) that appears in the right-click context menu (group `1_modification`) and forwards the active selection to the conversation's chat composer via the existing `sendbox.fill` event (consumed by every `*SendBox` platform component — `RemoteSendBox`, `AionrsSendBox`, `AcpSendBox`, `NanobotSendBox`, `OpenClawSendBox`). The payload is a fenced code block tagged with the file path, start/end line numbers, and the model's detected language so the model has enough context to act on the snippet. No-ops on a collapsed/empty selection so a bare right-click on a single cursor doesn't emit a stray empty message. (Used `sendbox.fill` rather than `sendbox.reply` because the editor has no chat `messageId` to attach a reply-quote to — `sendbox.reply` is reserved for the `SelectionReplyButton` reply flow; `sendbox.fill` is the renderer-wide "prepend text to the composer" hook.) `bunx tsc --noEmit` clean; oxlint 0 warnings/errors on the file.
- **Feat(editor): Phase 2 agent-editor integration (live stream + follow mode + change highlights).** Three pieces wired into the existing editor shell. (1) Live content stream: `MonacoEditor.tsx` subscribes to `ipcBridge.fileStream.contentUpdate` and, when the event targets the active buffer (matched on path + workspace), pushes the new content into the model via `model.pushEditOperations` so the undo stack is preserved — wrapped in the existing `suppressChangeRef` so the editor doesn't echo the change back out as a user edit. A buffer-dirty guard (`content !== originalContent`) prevents the agent from clobbering the user's unsaved typing; non-active open tabs still get their `content` + `originalContent` reconciled via the new `EditorContext.applyExternalContent(key, content)` so the dirty flag clears consistently. (2) Follow mode: `useWorkspaceEvents.ts` triggers `openEditorFile({ path, workspace })` + `expandEditor()` on the leading edge (`status === 'pending' | 'in_progress'`) of any `acp_tool_call` with `kind === 'read' | 'edit'`. File path is extracted from `rawInput.file_path` → `rawInput.path` → `locations[0].path` (mirrors `buildParamSummary`'s priority), guarded by an 80 ms per-path debounce so a single agent run that touches the same file repeatedly doesn't spam open requests. `openEditorFile` is idempotent — it short-circuits if the file is already open in any group, and the existing `pendingAction` flow handles dirty buffers. (3) Change highlights: a single `IEditorDecorationsCollection` per pane paints a brief line-level highlight (`--primary-6` tint) plus a glyph-margin bar on the lines the agent just modified, computed via a dependency-free longest-common-prefix + longest-common-suffix of LINES (O(n) scan, no LCS / Myers diff needed for typical edits). Decoration auto-clears on a 1.5 s timer and uses `NeverGrowsWhenTypingAtEdges` stickiness so it disappears as soon as the user types inside the region. CSS in `editor.css` (`.editor-agent-change-line` / `--glyph`), tokens via `--primary-6` so it tracks the active theme. `bunx tsc --noEmit` clean; `bun run lint:fix` 0 errors.

## 2026-06-07

### AionUi

- **Fix(editor): restore editor init after the build-fix regression ("language services failed to start").** The worker-factory swap in the build fix (custom `MonacoEnvironment.getWorker` + `?worker` Codingame editor worker) made `MonacoVscodeApiWrapper.start()` throw at runtime, so the editor only showed the `lspInitFailed` fallback. Reverted to the runtime-proven `configureDefaultWorkerFactory` (the config that worked in dev at `monaco-languageclient@10.7`). The production build stays green because the bare-`new URL('@codingame/…', import.meta.url)` worker resolution is handled by the Vite `resolveId` plugin below — the factory change was unnecessary once the plugin existed.
- **Fix(build): production bundle compiles again with `monaco-languageclient@10.7`.** The 10.7 upgrade (from the duplicate-`monaco-vscode-api` fix) broke `bun run package`: its worker loaders use `new URL('@codingame/...', import.meta.url)` with a bare specifier, which Rollup mis-joined to `.../lib/worker/@codingame/...` and failed to resolve; and `lib/vscode/config.js` imports the bare `@codingame/monaco-vscode-editor-api`, which isn't a top-level package. Two fixes in `electron.vite.config.ts`: (1) a `pre` `resolveId` plugin (`mlc-worker-url-resolve`) that rewrites the mis-joined `…/lib/worker/@codingame/<x>` ids back to the real `@codingame/<x>` module; (2) an alias `@codingame/monaco-vscode-editor-api` → the same 25.1.2 build as `@aionui/editor-monaco` (single copy). `bun run package` succeeds (renderer `vendor-editor` ~10.9 MB); editor tests still 71 green. (Caught by "did you compile all code?" — prior green runs were typecheck-only.)
- **Feat(editor): Epic C Phase 2 — N-way split + drag tabs between panes.** Lifted the two-pane cap: `splitEditor` now appends a new pane after the focused one up to `MAX_EDITOR_GROUPS` (4); past the cap it cycles focus instead of stacking slivers. Cmd/Ctrl+\\ and the View → "Split editor" action always add a pane; panes are closed via each pane's close-split affordance. New `moveBufferToGroup(bufferKey, fromGroupId, toGroupId, index?)` context action moves a tab between panes (buffer stays in the shared pool, so dirty state/content/undo survive); emptying a source pane drops it via `normalizeGroups`; moving a file already open in the target just activates it (no duplicate). `EditorTabs` now carries the source group + key over the DnD `dataTransfer` (`application/x-aionui-editor-tab`) so a drop in a _different_ pane's strip (or empty strip space — breadcrumb / pill / expert strip) moves the tab there; same-group drops still reorder. Sizing: exactly two panes keep the draggable ratio; 3+ panes distribute evenly with static dividers (per-divider N-pane resize deferred). +5 tests for `moveBufferToGroup` and the N-split/cap behavior in `tests/unit/renderer/conversation/editorGroups.dom.test.tsx` (15 total); `bunx tsc --noEmit` clean.
- **Feat(editor): Epic C — split editor (Phase 1: two side-by-side panes).** Added a VS Code-style split so the command-center editor can show two files at once. State model (per the oracle design): a shared `buffers` pool plus `groups: EditorGroup[]` + `activeGroupId` on `EditorState`; the top-level `activeKey` is kept in sync with the focused group's active tab so every existing single-group consumer (save/close/content/disk-poll/reveal) keeps working unchanged. New context actions: `splitEditor`, `closeGroup`, `focusGroup`, `setActiveBufferInGroup`, `reorderWithinGroup`, `requestCloseBufferInGroup`, `setBufferContentByKey`, `setSplitLayout`; legacy `setActiveBuffer`/`reorderBuffers`/`requestCloseBuffer` now delegate to the focused group. A new `EditorGroupView` owns each pane's own Monaco instance, tab strip, git-gutter decorations (`useEditorGitDecorations`) and LSP attachment (`useLspBridge`); `EditorPanel` is now a shell that renders the groups with a `useResizableSplit` divider and points the toolbar / status bar / outline / problems at the focused group via a per-group handle registry. `EditorTabs` is group-scoped (optional `groupId`/`isFocused`, backward-compatible defaults; global Cmd/Ctrl+W / Cmd/Ctrl+Tab fire only for the focused strip). Toolbar View menu + Cmd/Ctrl+\\ toggle split; each pane has a close-split affordance. Closing a tab only removes the buffer from the pool when no other group references it; emptying a group drops the pane; emptying the last group closes the editor. Tab persistence is backward-compatible: a `groups` field (serialized by file path) is added only when a split exists; legacy data reads as a single group; `useEditorTabsHydration` rebuilds the split with deterministic buffer keys. New i18n keys `conversation.editor.splitEditor` / `closeSplit` (8 locales). 10 new tests in `tests/unit/renderer/conversation/editorGroups.dom.test.tsx`; `bunx tsc --noEmit` clean. Phase 2 (N>2 panes, drag-to-move between groups) intentionally deferred.
- **Fix(editor): editor no longer renders as a white surface — `applyTheme(mode)` drives the active palette.** Two compounding bugs made the editor mount as a plain white background regardless of the user's light/dark preference. (1) In the workbench-theme-service path the `IStandaloneThemeService` override lacks `defineTheme`, so `monaco.editor.defineTheme(aionui-light, …)` was never called — the aionui themes don't exist in the registry. `MonacoEditor.tsx` then called `monaco.editor.setTheme('aionui-dark')` (via `themeNameFor`) which silently no-ops against an unknown name, and Monaco falls back to the default white `vs` surface. (2) The workbench fallback inside `defineTheme` was writing `workbench.colorCustomizations` once per call (light + dark, fired by `ensureAionuiThemesRegistered`). Because `updateUserConfiguration` is a full-file read-modify-write, the two writes raced and the second clobbered the first — only one mode's palette ever landed. `monacoTheme.ts` now (a) centralizes the user-config write in a new exported async `applyTheme(mode)` that sets `workbench.colorTheme` to the matching built-in base (`vs` / `vs-dark`, guaranteed to exist because `monacoVscodeApiInit` boots with `loadThemes: false`) and writes the full `workbench.colorCustomizations` palette derived from Chisl's CSS-variable tokens; (b) makes `defineTheme`'s workbench path purely synchronous (just `monaco.editor.setTheme(base)`) so it no longer races; (c) exports `initialThemeFor(mode)` so `monaco.editor.create({ theme })` picks the registered aionui name on the standalone path and the built-in base on the workbench path, preventing the white flash before `applyTheme` lands. `MonacoEditor.tsx` now calls `applyTheme(theme.mode)` in the mount effect and the theme follow-through effect, replacing `monaco.editor.setTheme(themeNameFor(…))` so the active palette is the one the user actually selected. The 13 tests in `tests/unit/renderer/conversation/monacoTheme.test.ts` now cover `applyTheme` in all three modes (workbench success / workbench write rejects / workbench import rejects / standalone) and the `initialThemeFor` matrix; `bunx tsc --noEmit` and `bun run format` exit 0; all 56 `tests/unit/renderer/conversation/` tests pass.
- **Fix(editor): make `defineTheme` tolerant of the workbench theme service (no more "Editor unavailable").** After `MonacoVscodeApiWrapper.start()` the service bound to `IStandaloneThemeService` may be a workbench override that lacks `defineTheme`, which threw `standaloneThemeService.defineTheme is not a function` and marked the editor unavailable. `monacoTheme.ts` now probes `StandaloneServices.get(IStandaloneThemeService)` and, if `defineTheme` is missing, falls back to `monaco.editor.setTheme(base)` — with a try/catch safety net around the real call for unknown override shapes. `monacoVscodeApiInit.ts` calls the defensive `ensureAionuiThemesRegistered()` right after `wrapper.start()` (both extended and classic fallback paths) so the editor surface always has a theme before `MonacoEditor` mounts. `loadThemes: false` and the `@aionui/editor-monaco` alias / Codingame 25.1.2 stack are unchanged; 5 new defensive-behavior tests in `tests/unit/renderer/conversation/monacoTheme.test.ts`; existing `lspSessionManager` / `lspLanguageId` / `editorSettings` / `editorLanguage` tests still green; `bunx tsc --noEmit` exits 0.
- **Fix(editor): duplicate `monaco-vscode-api` version ("Editor unavailable").** `monaco-languageclient` was 10.4 (Codingame 23.3) while the app used 25.1.2; extended→classic retry loaded two API builds. Upgraded to `monaco-languageclient@10.7.0`, bun overrides + Vite dedupe, dynamic import of vscodeApiWrapper in init only, removed second init in `MonacoEditorGate`, no classic retry on "already been loaded".
- **Fix(editor): prevent white-page crash in lazy editor chunk (keeps Epic B).** Root cause: any throw inside the lazy `editorLazyEntry` chunk — Codingame `MonacoVscodeApiWrapper` `start()` failure, mismatched Codingame worker URLs, or a render-phase exception in `MonacoEditor` after init — propagated past `React.Suspense` and unmounted the whole tree, leaving a blank white page. Three layered fixes: (1) New `ErrorBoundary` component (`@/renderer/components/base/ErrorBoundary.tsx`) wraps `<EditorLazyEntry>` in both `ChatLayout` and `EditorPane`; on catch it `console.error`s with a `[ChatLayout]/[EditorPane]` prefix and renders a graceful fallback (error message + Retry button) so the rest of the chat/workspace panes stay interactive. (2) `editorLazyEntry` now `import()`s `monacoVscodeApiInit` lazily inside its effect (so the heavy Codingame graph isn't pulled into the chunk's top-level evaluation), wraps the init in try/catch with a `[editorLazyEntry]` console.error prefix, and renders the existing `conversation.editor.lspInitFailed` surface on failure. `monacoVscodeApiInit` first attempts the `extended` `$type` wrapper (current happy path) and, if `wrapper.start()` throws, falls back to the `classic` `$type` so the editor still mounts when the views/extension-host service set is broken. (3) `MonacoEditorGate` now re-awaits `ensureMonacoVscodeApiInitialized()` before forwarding to `MonacoEditor`, so a stale ref-path render (HMR, hot reload) can't call `monaco.editor.create()` before the Codingame global is initialised. `useLspBridge` adds a defensive `.catch` on `attachLspForBuffer(...).then(...)` so a throw in the result handler (e.g. an `onStatus` consumer) never bubbles up as an unhandled rejection — the bridge just falls back to `unavailable / api`. No Monaco/alias/LSP code removed; Epic A tabs/git-gutter/settings still in place; Epic B LSP path preserved. 5 new ErrorBoundary tests in `tests/unit/renderer/components/ErrorBoundary.dom.test.tsx`; existing `lspSessionManager.test.ts` and `lspLanguageId.test.ts` still green; `bunx tsc --noEmit` and `bun run lint:fix` exit 0.
- **Feat(editor): Epic B (re-implementation) — lazy editor chunk + local LSP.** Editor UI loads only via `React.lazy('./editorLazyEntry')` from `ChatLayout` and `EditorPane`; bootstrap barrel no longer exports `EditorPanel` or `useEditorGitDecorations`. Codingame stack (`MonacoVscodeApiWrapper`, `@aionui/editor-monaco` alias) initializes inside the lazy chunk; `lspSessionManager` + `useLspBridge` attach `monaco-languageclient` to `aionui-lsp` over `/api/lsp/ws/:id`. Root `monaco-editor@0.52` stays for non-editor code paths. Vite `worker.format: 'es'` fixes production bundle for Codingame worker URLs. Unit tests: `lspSessionManager.test.ts`. `conversation.editor.lspInitFailed` i18n (8 locales).
- \*\*Fix(editor): revert Epic B Codingame `monaco-editor` alias — restored `monaco-editor@0.52` so the renderer bootstraps again (aliasing to `@codingame/monaco-vscode-editor-api` caused a blank page). Epic A (tabs, git gutter, settings) retained; LSP stubbed until lazy-loaded integration.
- **Feat(editor): Epic B — local LSP (`@codingame/monaco-vscode-api` v25 + `monaco-languageclient`).** _(rolled back — see fix above)_ `MonacoEditorGate` runs one-time VS Code API `initialize()` before first editor; `useLspBridge` + `lspSessionManager` connect to `aionui-lsp` over `/api/lsp/ws/:id` per workspace/language; install banner via `lspNotInstalled`; `EditorProblems` lists markers in expert aside. Vite uses `@codingame/esbuild-import-meta-url-plugin`. `monaco-editor` / `vscode` npm aliases point at Codingame packages.
- **Feat(editor): Epic A — engineering-ready editor baseline (Phase 0).** Per-workspace persisted editor prefs (`chisl.editor.prefs.*`: font, tabs, wrap, minimap, format-on-save) and open file tabs (`chisl.editor.tabs.*` keyed by workspace root). Tab hydration on `EditorPanel` mount in command-center via `useEditorTabsHydration` + `workspaceRoot` from ChatLayout. Git working-tree gutter on Monaco via `getDiff` + `gitDecorationsFromPatch` + `useEditorGitDecorations` (refreshes on `git.changed`). Editor ↔ file tree sync: `editor.reveal.path` event, `useEditorRevealInTree`, clickable breadcrumb segments. Format-on-save runs Monaco `formatDocument` through `saveEditorFile({ format })`. Layout modes and dual EditorPanel hosts unchanged.
- **Fix(ui): files/diff fly-out scales to modal height.** Full flex chain on `AionModal` + `git-diff-flyout-modal__workspace` so embedded `ChatWorkspace` fills `min(85vh, 900px)` and scrolls inside (commit box and tree no longer clipped at bottom).
- **Fix(ui): left Sider files/diff — match ConversationPane chrome.** Shared `SiderWorkspacePanel.module.css` (44px header, 13px/600 title, `bg-2` + `border bg-3`, 28×28 action buttons). Files and diff both use `SiderWorkspaceSectionHeader` + `SiderWorkspaceActionBtn`; diff toolbar removed when embedded; files tab strip uses same divider language. Right ConversationPane unchanged (reference only).
- **Fix(ui): file-tree “+” hover — rust fill + delayed tooltip.** Hover uses solid `--brand` background with cream icon; Arco `Tooltip` shows `addFileToChatTooltip` (“Add file to chat”) after 650ms dwell.
- **Fix(ui): left Sider workspace audit — Chisl branding.** Unified files/diff panes via `SiderWorkspaceSectionHeader`; removed duplicate Diff header; retired Zed/Linear dark fallbacks in `workspace.css` for Chisl tokens. Unified files/diff panes via `SiderWorkspaceSectionHeader` (8px padding, `border-b-base`, 12px type); removed duplicate Diff header in `SiderDiffSection`; embedded git toolbar shows section label + icon. Retired Zed/Linear dark fallbacks in `workspace.css` (`#ffffff`, `rgba(255,255,255,0.04)`) for Chisl `--text-*` / `--bg-hover` / `--brand` selection accent. Removed Tailwind `blue-*` paste badge and slate drag overlay; flyout modal CSS uses semantic tokens. Added `conversation.workspace.files.*` i18n (8 locales). Fixed hardcoded English empty state in `SiderFileTree`.
- Added a full-screen fly-out modal for the Git diff view to improve the source control review experience

### AionUi

- **Fix(ui): Sider diff expand uses Chisl `sider-action-btn` tokens.** Icon chip now matches conversation row / sider toolbar actions (`text-t-secondary`, opaque `bg-2` + border) so it is visible at rest in light and dark themes—not washed-out white until hover.
- **Fix(ui): Sider diff expand is a header corner icon, not a full-width button.** Moved fly-out trigger into the Diff title bar as a quiet icon-only control with tooltip; removed the secondary bordered row.
- **Fix(workspace): stop forcing Changes tab on every conversation load in Sider file tree.** A stray `useEffect` called `setActiveTab('changes')` whenever `conversation_id` changed for any non-`changes` panel, including `panelMode="files"` (top Sider pane). That hid the Arco file tree and showed `GitChangeList` ("0 file(s) changed / No changes yet") in the tree slot. Restored event-driven tab switch on `WORKSPACE_OPEN_REMOTE_CHANGES_EVENT` for `full` mode only; reset to `files` on conversation change; `files` / `changes` Sider panes unchanged.
- **Fix(git): register `git.*` channels on the IPC adapter allowlist.** `git.repo-info`, `git.status`, and the rest of the `ipcBridge.git` namespace were missing from `AdapterEventMap` / `ADAPTER_EVENT_NAMES`, so renderer invokes were rejected or never reached `initGitBridge` providers — surfacing as endless loading and `git.getRepoInfo timed out after 15000ms`. Added all git provider events plus `git.changed` outbound typing; extended `events.test.ts`.
- **Chore(backend): default `AIONUI_DISABLE_TOOL_SNAPSHOT=1` for spawned aioncore.** `buildSpawnEnv` in `packages/web-host` passes the flag through (overridable via parent `process.env`) so Electron desktop runs disable the per-tool-call ledger without extra user setup.
- **Refactor(workspace): delete legacy fileSnapshot machinery in favor of GitChangeList + useGitChanges.** Removed the entire `ipcBridge.fileSnapshot.*` surface (init/compare/baseline/stage/unstage/discard/reset/branches/dispose) along with the snake_case `fromBackendCompareResult` mapper and the `FileChangeOperation` / `FileChangeInfo` / `CompareResult` / `SnapshotInfo` types. Deleted `useFileChanges` (renderer hook), `FileChangeList` (renderer component), `useRemoteSessionChanges` and `remoteSessionDiffMapper` (only consumer was the removed remote branch in `Workspace/index.tsx`). `ChatLayout` now drives the header change-count strip with `useGitChanges(workspacePath, workspaceEnabled)` directly — the 5-second `setInterval` poll is gone because the hook subscribes to `ipcBridge.git.changed` internally. `Workspace/index.tsx` is git-only: no more `isRemoteChanges` ternaries, `WORKSPACE_OPEN_REMOTE_CHANGES_EVENT` source switching, or `FileChangeList`/`useRemoteSessionChanges` imports. `SiderDiffSection` and `Workspace/types.ts` comments now reference `GitChangeList`; `meta.isRemote` stays in the type as a frozen `false` for API stability. E2E bridge `fs.snapshot.*` routes and the `snapshotCompare` response mapper are removed; `workspace-snapshot.e2e.ts` now `git init -q` the temp workspace in `beforeAll` (and the headers/comments are updated to talk about the Git IPC path). `bunx tsc --noEmit`, `bun run lint:fix`, `bun run i18n:types`, and `node scripts/check-i18n.js` all pass.
- **Fix(workspace): address race conditions and cache staleness in Git Diff panel.** Fixed an issue where stale async `getRepoInfo`/`getStatus` results could overwrite newer state during rapid workspace switching by adding a generation request guard (`useRef`) and an unmount check in `useGitChanges`. Fixed memory/event leaks by releasing the main-process file watcher (`ipcBridge.git.unwatch.invoke`) on unmount or workspace change. Improved file-change event matching to rely on `repoRoot` (enabling sibling-file refresh in subdirectory workspaces). Added `try/catch` wrappers around all `ipcBridge.git` invocations (e.g., `handleAction`, `getDiff`) to surface transport rejections and prevent unhandled promise errors in the UI. Updated `GitChangeList` to reliably clear its diff cache when the workspace or status version changes, ensuring users see the latest diffs after edits or staging. Untracked files are now expandable, utilizing the main-process's synthesized all-additions patches.
- **Fix(git-service): correct critical correctness bugs in main-process Git service/bridge.** Five fixes: (1) **`gitStatusMapper` now derives state from porcelain per-file codes** (`status.files[*].index` / `working_dir`) instead of the conflated `status.modified`/`staged` arrays, so a staged-only modified file is reported in the STAGED bucket only (previously it also appeared as unstaged). All conflict codes (`U`, `AA`, `DD`, `AU`, `UA`, `UD`, `DU`, `UU`) and the `??` untracked pair are recognized; partially-staged files correctly appear in BOTH buckets. (2) **`discardFile` now matches VS Code "Discard Changes" semantics**: untracked files are unlinked, tracked working-tree changes are reverted via `git restore --worktree` (preserves any staged content), and `git rm --cached` is now ONLY used for the legitimate unborn-repo staged-addition case — never against a normal tracked staged file. (3) **Watcher now observes the RESOLVED REPO ROOT** (not the raw workspace) with per-root refcounting, so opening a subdirectory still surfaces sibling changes and `ensureWatch`/`unwatch` are properly paired via `Map<root, WatcherEntry>`. The `ipcBridge.git.unwatch` provider is registered; `changed` events now carry `{ workspace, root }` matching the updated `GitChangedEvent` contract. (4) **`commit()` flips the `hasCommits` cache to true** for the resolved root, so the first successful commit no longer leaves the cache stuck at "no commits" and pushes subsequent `discardFile`/`unstageFile` calls onto the wrong (unborn-repo) code path. The cache is also re-probed on misses so it cannot get permanently stuck. (5) **`getStatus` reuses the branch from `getRepoInfo`** instead of `status.current`, so a freshly-initialized (unborn) repo reports `branch:null` consistently across `getRepoInfo` and `getStatus`. Mapper gains an optional 5th parameter (`infoBranch`) so the service can pass the already-resolved branch through. `bunx tsc --noEmit` and `bun run lint` exit 0; `bun run test tests/unit/git` runs 102/102 green (was 74; +20 new mapper tests, +8 new service/bridge tests).

### AionCore

- **Chore(git): gate per-tool-call snapshot ledger behind `AIONUI_DISABLE_TOOL_SNAPSHOT`.** Desktop `GitService` is the sole SCM/diff surface; when the env var is `1`/`true`, `agent.rs` passes `SnapshotHook = None` into OpenCode MCP registration and `build_file_state` skips wiring `snapshot_deps` for agent + conversation routes. Stops phantom `tool_call: N` commits on the user's real `master` branch while leaving `/api/fs/snapshot/*` routes in place for any remaining callers until a follow-up delete.
- **Milestone #7: remote shell approval and local-fs MCP hardening.** Aligns shell-approval wait with OpenCode's MCP tool timeout (`MCP_TOOL_TIMEOUT_MS` / `SHELL_APPROVAL_WAIT_MS`), replays pending confirmations after SSE reconnect, infers sub-agent session routing when OpenCode omits session headers, and extends `local_fs_mcp` server/protocol/shell/tools for safer nested-session handling. Adds integration test `opencode_mcp_registration.rs` for MCP registration round-trip.

### AionUi

- **Feat(workspace): rebuild Git Diff panel on IPC contract.** Rebuilt `GitChangeList` (formerly `FileChangeList`) on top of the native `ipcBridge.git` contract, decoupling the local renderer from the old `fileSnapshot` backend. Added explicit UI states for `git-not-installed` and `no-repo` with an "Initialize Git repository" flow. Implemented a commit box (message + commit action), per-file discard confirmations, and replaced legacy session polling with debounced subscriptions to `ipcBridge.git.changed`. Repointed `SiderDiffSection` and `Workspace/index.tsx` to the new `useGitChanges` hook while leaving the remote-session mode unchanged. Added 20+ missing i18n keys across all 8 locales for the new Git terminology. All tsc/lint/i18n checks pass.

- **Milestone #7: conversation pane and workspace tree polish.** Adds `WorkspaceFileIcon`, refines workspace tree interactions (context menu, hover add-to-chat, smart open routing), enables workspace file trees for all conversation types carrying `extra.workspace`, and updates conversation-pane layout/styling plus related remote-session and message UI tweaks across the renderer. Process-layer approval/indexer/queue/registry modules and unit tests updated in the same pass; `monaco-editor` dependency order normalized in `package.json`.

### AionUi

- **Fix(workspace): align file tree interactions with IDE conventions.** Three changes: (1) **Double-click a file now opens it in the in-app editor** (text/code) or the OS default app (binary/visual) via the existing `handlePreviewFile` smart-routing pipeline, instead of mis-firing the "add to chat" path. Folder double-click still falls through to the tree's default expand behavior. (2) **Inline "Add to chat" icon button** added to every tree row (right of the name), visible only on hover/focus via a `group/group-hover:opacity-100` + `group-focus-within:opacity-100` transition, with a 20×20px rounded button, subtle `text-t-tertiary → hover:text-t-primary` color shift, `hover:bg-fill-3` background, native `title`/`aria-label` tooltip, and `MouseDown.stopPropagation` so the click doesn't also toggle row selection. Uses the `MessageOne` icon from `@icon-park/react`. Mobile still shows the existing "more" kebab button in the same slot. (3) **Context menu cleaned up**: the redundant "Preview" entry (it duplicated the "Open" path) is removed; the "Open" entry now calls `handlePreviewFile` for files (smart routing: text → editor, binary → OS default) and `handleOpenNode` for folders (file manager), so the in-app editor is the default opener for text/code files instead of the OS shell. The final menu order is: Add to chat → Open → Show in folder → Download (files only) → ── → Delete → Rename, matching standard IDE context menus. `isPreviewSupportedExt` import removed from `WorkspaceContextMenu.tsx` since it's no longer used. `bunx tsc --noEmit` and `node scripts/check-i18n.js` pass; no i18n key changes (the `preview` key is still used by `previewError.ts`). No commit, no push.
- **Feat(workspace): render file tree for every conversation type that carries a workspace.** `ChatSlider` previously hard-coded the four conversation types (`acp`, `codex`, `aionrs`, `remote`) that could host a `ChatWorkspace`, leaving `openclaw-gateway`, `nanobot`, and legacy `gemini` conversations without a file tree even when `extra.workspace` was set. Replaced the four-way `if/else` chain with a single `conversation.extra?.workspace && conversation.type` branch that uses the conversation type itself as the `eventPrefix`, so every `TChatConversation` variant (`acp`, `codex`, `openclaw-gateway`, `gemini`, `nanobot`, `remote`, `aionrs`) now renders the workspace tree when a workspace path is present. Introduced a shared `WorkspaceEventPrefix = TChatConversation['type']` alias in `Workspace/types.ts` and reused it in `useWorkspaceTree` / `useWorkspaceEvents` / `useWorkspaceFileOps` so the three hooks can no longer drift from the prop type. The emitter's `EventTypes` interface is now generated from the same conversation-type union via a template-literal mapped type (`${prefix}.selected.file` / `.append` / `.clear` / `workspace.refresh`), which automatically wires the previously-missing `gemini.*` events. The Arco `<Tree>` implementation, lazy-loading, and workspace event protocol are unchanged. `bunx tsc --noEmit` passes. No commit, no push.

## 2026-06-05

### AionUi

- **Fix(terminal): restore default-mode panel sizing and imperative resize sync.** `TerminalPanelHost` no longer treats every layout mode (including `default`) as a mode-driven pane override, so opening the terminal uses persisted `panel.heightPct` again instead of layout-mode defaults that could collapse to 0 height. Split-pane still applies layout-mode pane ratios, clamps corrupt/zero sizes to `MIN_TERM_PCT`, and mode transitions now update both `TerminalPanelContext.heightPct` and the `react-resizable-panels` imperative handle. Unit tests added in `tests/unit/renderer/TerminalPanelHost.dom.test.tsx`. No commit, no push.

### AionUi

- **Feat(t2-registry-01 / t2-registry-03): persistent JSON registry storage and secure credential foundation.** Adds file-backed registry persistence under `packages/desktop/src/process/services/registry/`: `paths.ts` resolves `chisl-servers.json` in `userData`; `safeStorageEncryptor.ts` wraps Electron `safeStorage.encryptString`/`decryptString` when available in main process, falling back to a volatile in-memory encryptor stub for tests and non-Electron environments; `fileStore.ts` provides `createChislServerRegistryFileStore` (JSON snapshot read/write with plaintext-password refusal guard) and `createChislServerCredentialFileStore` (encrypted credential sidecar keyed by normalized URL, encrypt-on-flush / decrypt-on-load via the encryptor interface). Registry snapshot JSON stores only `url, username, displayName, hasPassword, authToken` — never plaintext passwords. Credential sidecar stores only encrypted blobs. Integration test verifies end-to-end: registry metadata persists without secrets, credential sidecar encrypts/decrypts independently. 21 unit tests in `tests/unit/process/registry/` (paths, encryptor, file store, integration). No new dependency, no commit, no push.

### AionUi

- **Feat(forge-5-03-04): command safety matrix foundation integrated with approval rules.** Adds pure command-safety classification under `packages/desktop/src/process/services/approval/commandSafety/` with decisions `allow_once`, `manual`, and `deny`; hazard/reason model; workspace-contained path checks (external, dynamic, secret paths); conservative classifier for shell command strings and permission `patterns` covering the approved 16 ALLOW_ONCE commands, constrained file-read/search (`cat`, `head`, `tail`, `wc`, `grep`, `rg`, `find` with `-exec`/`-delete` denied), manual `env`/`printenv`/`time`, destructive/mutating deny list (filesystem, git, package managers, mutating curl/wget), and structural hazards (chaining, pipes, redirection writes, command substitution, pipe-to-shell). Integration helpers (`suggestApprovalFromCommandSafety`, `buildSuggestedApprovalRuleInput`) map classifications to suggested `ApprovalRule` actions only; no permission replies sent, no queue mutation, no OpenCode calls; default-deny preserved. Unit tests in `tests/unit/process/approval/commandSafety.test.ts`. No commit, no push.

### AionCore

- **Feat(forge-5-02-03): read-only restore-plan route/service wiring on the existing snapshot ledger.** Exposes the previously-added `ToolCallRestorePlan` through the per-conversation snapshot service and a new read-only conversation route, returning the computed plan (per-path operations, restorability, preview-block flag, parent commit SHA, warnings/errors) built from an existing `opencode_tool_snapshots` ledger row + `git2::Repository`. Reuses the existing ledger; adds no new table, dependency, or parallel ledger. The route does not mutate the working tree, does not execute any restore, and does not call OpenCode. Restore execution remains deferred to forge-5-03. Restore-plan route test added; no commit, no push.

### AionUi

- **Feat(forge-5-03-03): Chisl-owned approval rule schema, evaluator, and audit foundation.** Adds the Chisl approval-rule module under `packages/desktop/src/process/services/approval/` with `ApprovalRule` (id, name, scope session|workspace|global, scopeRef, tool, matcher JSON, action allow|deny|ask, priority, expiry, enabled, createdBy, createdAt, updatedAt, reason, tags), `ChislPermissionRequest`, `ApprovalEvaluationContext`, matcher types (exact, glob, regex, prefix, jsonpath via dot-path, composite and/or), pure evaluator that excludes disabled/expired rules, orders by session > workspace > global, higher priority first, older `createdAt` then `id` as tie-break, gives `deny` precedence over allow/ask, falls back to first-match among non-deny rules, and defaults to **DENY** (`replySent: 'reject'`) when no rule matches; allow maps to `once`, deny maps to `reject`, ask/manual maps to `none`; immutable `ApprovalAudit` records (requestId, sessionId, permission, patterns, metadata snapshot, decision allow|deny|manual|fallback, ruleId/name/scope, replySent, endpointUsed, reason, evaluatedAt, evaluationMs, principal, ruleSnapshot) include `replySent: 'once'` for allow decisions. Durable persistence via a new Chisl-local `chisl-approval.db` at `userData/chisl-approval.db` (separate from `aionui.db`, `chisl-index.db`, `chisl-queue.db`) with `approval_rules` and `approval_audits` tables and CRUD/append helpers. Foundation only: no live permission replies sent, no OpenCode endpoint calls, no IPC/UI wiring, no command safety matrix (deferred to forge-5-03-04). No new dependency, no commit, no push.

### AionUi

- **Feat(forge-5-02-03): read-only restore-plan preview bridge/UI.** Adds an IPC bridge call to fetch the read-only restore plan and a `RestorePlanPreview` component surfaced from the tool-call message UI (`MessageAcpToolCall`, `MessageToolGroupSummary`), rendering per-path operations, restorability, and preview/warning state with new `en-US` i18n keys. Preview only: no restore execution, no working-tree mutation, no OpenCode call; restore execution remains deferred to forge-5-03. No commit, no push.

## 2026-06-04

### AionCore

- **Feat(forge-5-02 gap-fill): restore-plan API + hook observability on the existing `opencode_tool_snapshots` ledger.** Adds a pure, testable `ToolCallRestorePlan` (per-path operation `create`/`modify`/`delete`/`unknown`, prior-content restorability, binary/UTF-8 preview-block flag, parent commit SHA, warnings/errors) built from an existing ledger row + a `git2::Repository`, with no new DB table, no new dependency, and no new parallel ledger. `SnapshotService::tool_call_restore_plan_from_ledger` exposes the plan from the existing per-conversation service. Reuses the same parent-tree semantics as `revert_files_from_commit`; does not mutate the working tree and does not call OpenCode. `LedgerHookOutcome` (tool_call_id, conversation_id, stage, success, files_count, commit_sha, error, warning) replaces silent swallow in `commit_tool_snapshot_after` with structured `tracing` events at every stage (`init`/`commit`/`persist`/`complete`/`skipped`); the tool's success path is unchanged. Explicitly documents and re-asserts the slice's coverage: `run_shell` is not snapshotted, non-`local_fs_mcp` mutations are not covered, OpenCode session-state revert is out of scope — surfaced as `RestorePlanUnsupportedCoverage::documented_defaults()`. 6 restore-plan unit tests + 2 ledger-hook observability tests added; 187 aionui-file + 630 aionui-ai-agent lib tests pass. No new dependency, no commit, no push.

### AionUi

### AionUi

- **Feat(forge-5-01-03): Chisl-owned durable queue state foundation.** Adds Chisl-local `chisl-queue.db` at `userData/chisl-queue.db` (separate from `aionui.db` and `chisl-index.db`) with schema for `queue_items` and `queue_session_counters`. Queue item types cover id, conditional sessionID, optional messageID, commandType (`create_session`, `prompt`, `abort`, `delete_session`, `permission_reply` placeholder), payload, per-session order, status (`queued`, `dispatching`, `running`, `blocked`, `failed`, `cancelled`, `complete`), timestamps, retry fields, cancellation fields, parentID, and metadata. Pure state-machine helpers enforce strict same-session serialization, cross-session dispatchability up to global limit (default 5), retry classification, cancellation (local queued cancel + abort path for running/blocked), and recovery bootstrap that does not treat `/session/status` absence as completion. No live OpenCode dispatch, no v2 queue, no worker execution. Unit tests in `tests/unit/process/queue/`.
- **Feat(indexer-01-04): Chisl-owned filesystem watcher and invalidation foundation.** Adds Chisl-local watcher/invalidation modules on top of the indexer store: `ignore.ts` (safe defaults: `.git`, `node_modules`, `dist`/`out`/`build`/`.next`/`.turbo`/`coverage`/`target`, OS junk files, secret/credential file names/suffixes, and the chisl-index DB itself), `contentHash.ts` (SHA-256 file content hash helper), `debounce.ts` (configurable `createDebouncedBatch`), `invalidation.ts` (per-file `scheduleFileReindexIfChanged`, `scheduleFileDeleteInvalidation`, and batched `processFileChangeEvents` that diffs against `files.content_hash` and writes `index_jobs` rows for new/changed/deleted files), `startupScan.ts` (`runStartupIndexScan` fallback enumerator that schedules jobs only for missing or changed hashes), and `watcher.ts` (`ChislIndexWatcher` with debounced event emission, start/stop lifecycle, and a per-directory watch fallback when the platform lacks recursive `fs.watch`). No OpenCode or AionCore source changes, no new dependencies, no embeddings, no raw content leaving the local process. Unit tests in `tests/unit/process/indexer/ignore.test.ts`, `contentHash.test.ts`, `debounce.test.ts`, `invalidation.test.ts`, `startupScan.test.ts`, `watcher.test.ts`.
- **Feat(indexer-01-03): Chisl local SQLite index store foundation.** Adds Chisl-owned `chisl-index.db` at `userData/chisl-index.db` (separate from `aionui.db`) with schema for `files`, `chunks`, `symbols`, `embeddings`, and `index_jobs`. Repository helpers for file upsert/list, chunk/symbol replace/list, embedding BLOB storage with `Float32Array` serialization, and index job create/update/list. Embedding provider kept as interface/config only. Unit tests in `tests/unit/process/indexer/`.
- **Feat(t2-registry-03): HTTP server credential storage and routing foundation.** Adds in-memory credential store and hydration for HTTP server passwords without persisting plaintext in registry snapshots (`hasPassword` metadata preserved). Routing helpers for SDK config, SSE (`/global/event` + Basic auth), and PTY WebSocket URL construction. Headless env/flag credential parser (`CHISL_HTTP_SERVER_*`). Unit tests in `httpServerCredentials.test.ts`, `httpServerRouting.test.ts`, `httpServerHeadlessCredentials.test.ts`.
- **Feat(t2-registry-02): HTTP server health check and Basic auth for registry.** Atomic `t2-registry-02` from the Chisl track. Adds health probing and auth-header construction for HTTP OpenCode server records on top of the t2-registry-01 foundation without encrypted credential storage. New modules under `packages/desktop/src/common/registry/`: `httpServerAuth.ts` (Basic auth token + `Authorization` header only when password is truthy; username defaults to `opencode` when password exists without username) and `httpServerHealth.ts` (`GET /global/health` via `fetch`, timeout 3000ms, retries 2 with delay `100ms * (attempt + 1)`, cache TTL 750ms, poll constant 10s for future UI wiring). Health statuses: `unknown`, `checking`, `healthy`, `unhealthy`, `auth_failed` (401), `error` (network/timeout). Exports wired through `index.ts`. Unit coverage in `tests/unit/common/registry/httpServerAuth.test.ts` and `httpServerHealth.test.ts` (no-password header omission, Basic auth with default username, retry/cache/200/401/network). t2-registry-01 registry CRUD semantics unchanged. **Out of scope**: encrypted credential storage (t2-registry-03), authToken/SSE/PTY/sidecar/ssh, UI polling integration, live endpoint probes.
- **Feat(t2-registry-01): HTTP-only server connection registry foundation.**

### AionUi

- Restyled the `ChatWorkspace` file tree to have a denser, VSCode-like developer-tool aesthetic. Replaced floating selection pills with edge-to-edge subtle background colors, reduced line height to 22px, switched to standard right-chevrons for folder expansion, and added subtle hover-based vertical indent guides instead of heavy Arco-default connection lines.

## 2026-06-07

### AionUi

- Elevated the workspace file tree to a premium developer aesthetic with file-extension specific icons, rich folder states, subtle hover/selection gradients, and smooth micro-interactions.

### AionUi (2026-06-07)

- Rebuilt the renderer Diff panel on top of the new Git IPC contract (`ipcBridge.git`), decoupling it entirely from the old `fileSnapshot` backend for local changes.
- Added explicit state views for `git-not-installed` and `no-repo`, along with a dedicated "Initialize Git repository" flow.
- Replaced polling with debounced subscriptions to `ipcBridge.git.changed` for robust live refreshes.
- Introduced a built-in commit box that interacts directly with the workspace index.
- Added discard confirmations for untracked/staged files.
- Refactored `Workspace/index.tsx` to mount `<GitChangeList>` dynamically depending on local vs remote-session mode.

### AionUi

- **Feat(git): main-process Git service + IPC bridge for the desktop-side Git panel.** Implements `GitService` (main-process singleton wrapping `simple-git`) under `packages/desktop/src/process/services/git/` with pure helpers (`gitStatusMapper`, `pathUtils`, `constants` for default `.gitignore` / watcher debounce), the `gitBridge` IPC provider, registration in `initAllBridges`, and teardown in the existing `before-quit` path alongside `disposeTerminalBridge`. Decoupled from aioncore end-to-end: every method uses local `git` / `node:fs` only. Repo discovery uses `git rev-parse --show-toplevel` so opening a SUBDIRECTORY resolves the enclosing repo correctly (the bug that broke the previous implementation); detached HEAD and "no commits" return `branch:null`. Status mapper splits staged/unstaged/conflicted, with partially-staged files appearing in BOTH groups; numstat populates `additions`/`deletions` and binary flags. `init` runs `git init` in the user's workspace and writes a sensible default `.gitignore` only when one is missing (never overwrites, never auto-commits). `getDiff` returns a unified diff for staged/unstaged/untracked/binary, with synthesized all-additions patches for untracked files. `discardFile` is destructive: restores tracked modifications via `git restore`/`checkout` and removes untracked files from disk; on a no-commits repo, falls back to `git rm --cached`. A debounced chokidar watcher (`Map<workspace, FSWatcher>`, ignores `.git/`, `node_modules/`, build folders, ~300ms debounce) emits `'changed'` events that the bridge forwards to `ipcBridge.git.changed`. New deps: `simple-git@^3.27.0`, `chokidar@^3.6.0` (added in alphabetical order in `package.json` and `bun.lock`). Tests: 74/74 pass in `tests/unit/git/` (38 happy-path integration + 36 edge-case unit tests covering no-commits branches, watcher coalescing, path utils, and every bridge provider wrapper). Coverage on the new module: 88.35% statements / 90.19% lines (well above 80%). `bunx tsc --noEmit` and `bun run lint` exit 0 across the whole project (pre-existing lint warnings only).

## 2026-06-11 (AionUi / AionCore)

### Phase 3: SideCars, Background Processes, Voice Mode, and Event Reactions

**SideCar Proxy (WS3)**

- Added SideCar registry and embedded reverse proxy to AionCore (`router/sidecar.rs`). Securely exposes localhost dev services (e.g. OpenVSCode Server, ttyd) via a single authenticated origin with per-sidecar UUID/cookie authentication, hop-by-hop header stripping, and full WebSocket `Upgrade` bridging via `tokio-tungstenite`.
- Added SideCar settings UI to AionUi (`SidecarSettings.tsx`) to manage registered services (name, port).
- SideCars open in a dedicated full-height modal tab containing a locked-down `<webview>` element.
- Documented SideCar configuration, recipes, and security model in `docs/sidecars.md`.

**Background Processes (WS2)**

- Added `BgProcessManager` to AionCore plugin webserver. Spawns long-running shell processes behind interactive user approval, with resource limits (max 8 per agent, 512 KiB output ring buffer), timeout enforcements, and audit records.
- Exported five plugin tools (`bg_start`, `bg_stop`, `bg_list`, `bg_read`, `bg_tail`) inside `@chisl/chisl-opencode-plugin` v0.2.0. The `bg_tail` tool handles streaming SSE log reads safely.
- Added live Background Processes indicator to the AionUi conversation header (visible only when processes are running) and a drawer panel with a real-time updating terminal output viewer and process controls.
- Ensures all processes are cleanly terminated (orphans reaped) when the conversation is closed or the app shuts down.

**Voice Mode (WS1)**

- Upgraded the plugin channel to support sticky voice mode state (`POST /api/tts` config via `voice-mode` endpoint) and pushed state recovery.
- Introduced `VoiceModeStore` in the OpenCode plugin to inject a `spoken` block instruction into the model's system prompt dynamically.
- Implemented sequential per-message TTS playback queue (`TtsQueue`) in AionUi. Supports Web Speech API (built-in) and OpenAI (`POST /api/tts`) text-to-speech providers.
- Added Voice Mode toggle to the chat input box (remote conversations) and a compact playback control row (SpokenRow) to assistant messages containing TTS-friendly `<spoken>` fenced blocks.

**Event Reactions (WS4)**

- Replaced polling loops with push-based WebSocket event triggers.
- Forwarded OpenCode SDK events (`file.watcher.updated`, `session.idle`, `session.error`) across the plugin channel and emitted them to the AionUi front end (`remote.workspaceChanged`, `remote.sessionHealth`).
- Preview context and editor disk-change loops now refresh immediately on remote workspace file change events.
- Health indicators and badges now reflect `session.error` push events instantly with length-capped server error messages.

### Phase 3 fixups (post-review)

- **AionCore**: added negative security test `sc01b_non_loopback_client_is_refused_even_with_valid_token` proving the SideCar proxy hard-rejects (403) non-loopback clients even with a valid token — closes the untested-enforcement gap flagged in review.
- **AionUi**: fixed 3 lint errors (`no-useless-spread`) shipped in the new WS4 test files under a false "pre-existing" claim; `bun run lint` now exits 0.
- Phase-3-Report.md corrected: real commit hashes (was stale "Uncommitted"), SDK pin vs plugin-package version disambiguated, `sidecar.rs` location fixed, omitted oracle security review restored with per-finding disposition, owner live-QA script and sticky-session-error product question added.
